# AGENTS.md

本文件是 data0 中 AI agent 与贡献者的工作约定。语义承诺面以 [README.md](./README.md) 为准；代码、测试与文档发生冲突时，先确认可执行行为，再修改约定。

## 代码 review 的证据纪律

### 1. 真实复现优先

- 报告“已确认缺陷”前，必须在当前 checkout 上得到确定、可重复的可执行复现。
- 复现必须记录：触发条件、执行命令或测试、预期结果、实际结果，以及运行的是源码还是生产构建。**必须核对实际错误的类型与消息原文**，不得凭输出形状（如"很深的栈"）推断错误类别——历史上曾把断言错误误报为栈溢出。
- 现有测试通过、覆盖率高、代码看起来可疑，都不能单独证明缺陷存在。
- 修复前先让最小复现失败；修复后用同一复现证明通过。
- **负面断言纪律**：“X 没有问题”类结论必须写成“在方法 M 下未发现 X 的反例”，注明方法与其可达范围。review 只能给出下界，任何方法都无法证明缺陷不存在。

### 2. 严格区分证据等级

Review 结论必须分为三类，不得混写：

1. **动态复现**：实际执行后观察到错误结果、异常、泄漏或状态破坏。
2. **静态确认**：无需运行即可验证的仓库事实，例如缺少 CI、锁文件与 manifest 不一致；必须明确标注“未运行触发”。
3. **待验证假设**：只有可疑路径或触发条件尚未闭合；不得写成已确认问题，也不得给出确定影响。

如果一个问题包含多个影响声明，每个声明都要分别说明证据等级。例如“下标写错”可以静态确认，但“因此泄漏 effect”仍需生命周期复现。

### 3. 复现必须安全、确定且可转成回归测试

- 优先使用最小 Vitest 用例；集合/增量算法优先与原生数据结构或强制全量重算做差分验证。
- 随机测试必须固定 seed，并在失败信息中输出 seed、步骤和操作。
- 当前已知缺陷可以临时使用 `test.fails` 保存可执行证据；修复时必须将其改为普通测试，禁止永久跳过。
- 测试必须清理创建的 effect、timer、临时目录，并恢复 Notifier 等全局单例状态，不能污染其他用例。
- 安全问题必须在临时目录、假命令或 mock 环境中复现。禁止为证明问题而执行真实 publish、push、删库、外部请求或其他破坏性操作。

### 3.1 修复必须向上游还债（等价类规则）

每个缺陷修复除实例级回归测试外，必须回答并落实三件事：

1. **等价类是什么**：这个缺陷所属的输入/状态/调度类别（例如"重复原始值下按值定位"而不是"push(0) 顺序错"）。
2. **哪层防线补上了这个类**：类型/构造性原语、dev 不变量断言、差分/性质测试、交错枚举，至少一层要能覆盖整个等价类，并作为常驻资产（而非一次性验证）进入仓库。
3. **同一变更语义还有哪些入口**：判等/触发/守卫类语义的修复必须对该语义的**所有入口**过账（set 与 replace/replaceData、单条与批量、方法与构造采纳）。该轴由 `__tests__/entryPointSemanticsInventory.spec.ts` 机械强制：每个集合变更方法必须归入语义组，equalityGatedWrite 组的每个入口自动继承 Object.is 判等探针；豁免（RxList.set 的 EKC 协议——下游可能依赖 set(i, sameItem) 强制重建行，加判等门属协议变更须与 axii/axle 同步）必须显式登记并附特征钉扎。

只修被复现的实例、只钉住该实例的回归测试，视为未完成的修复——历史上"替换型 splice 的重复值错位"修复未覆盖"纯插入/set 的重复值错位"，缺陷类因此存活了整整一轮 review；"RxMap.set 的判等语义"修复（方法 18）沿值域轴泛化却漏掉 replace 入口，方法 19 才命中——**修复者天然沿被攻击的轴泛化，把携带缺陷的轴当成偶然背景**，第 3 问就是对这个反复出现的事故形状的机械防线。

### 3.2 深度 review 按方法立项

- 每轮深度 review 开始前必须声明：本轮引入哪种**此前未使用过**的方法（差分性质、对抗值域、调度交错枚举、mutation 审计、模型比对……）。
- 同一方法的重复审查只做回归确认，不计为新一轮深度 review。同方法多轮的边际产出趋近于零；发现新缺陷依赖方法类别的更换，而不是轮数的堆积。
- 已使用过的方法清单（新方法用毕后追加到此）：
  1. 通读源码 + 手写示例测试（早期各轮）；
  2. splice 参数域对抗（负数/越界/小数/NaN）与归一化横扫；
  3. 固定 seed 差分 fuzz（增量结果 ≡ 全量重算，含重复值域）；
  4. async 敌意调度与完成序交错枚举；
  5. 异常注入（订阅者/回调抛错后全局状态复原检查）；
  6. 生命周期审计（destroy/孤儿 effect/泄漏计数）；
  7. batch/延迟调度下的多 info 单次 digest 重放差分（既有差分 fuzz 全部在 batch 外逐操作断言，隐含"每次 digest 恰一条 info"的假设；重放语义本身是独立攻击面，尤其是 EXPLICIT_KEY_CHANGE 与结构操作混排）；
  8. destroy 僵尸行为横扫 + destroy 事件对称性检查（直接断言"destroy 后不再接收更新"；不依赖 retainedDiagnostics——它只统计 active=true 的 effect，源模式结构在其中完全不可见）。
  9. 生产构建（`__DEV__:false`）契约差分 + 对抗探针（object-atom 浅写、indexKeyDeps 惰性清扫、generator 异步重入、稀疏 set × map(index)、API/文档漂移）；资产：`__tests__/verifiedReviewFixes.spec.ts`（实例回归）、`__tests__/sparseSetOperatorsSweep.spec.ts`（"OOB set × 全派生算子族不崩溃且可恢复"的等价类横扫，含 batch 多 info 回退路径）。
  10. 既有攻击轴 × 未覆盖算子族的组合横扫（重复值域 × selection 家族、undefined 合法元素 × toSorted 的 EXPLICIT_KEY_CHANGE 路径、链式深层管道差分、回调重入）；发现并修复 createSelection 重复 item indicator 漂移（`itemToIndicators` 改 Set 广播 + 按身份精确移除）与 toSorted 变更含 undefined 时与全量 sort 分叉（回退全量重算）两个缺陷类；资产：`__tests__/deepReview2026H2Findings.spec.ts`（实例回归 + 两个等价类的常驻差分 sweep：重复 item 域 × selection 的"indicator ≡ currentValues 成员"不变量、undefined 值域 × toSorted 的"增量 ≡ 全量 sort"不变量，双 compare 形态）。既有 fuzz 的盲区模式：差分 fuzz 只喂"单一算子 × 唯一/重复数值域"，selection（值→indicator 的 Map 记账）与"undefined 作为合法元素值"两个维度都不在任何生成器的值域里。
  11. 模型比对（系统级管道网 ≡ 朴素参考模型）：多源 × 多派生 × 链式管道 × selection × RxSet/RxMap 派生组成一张网，随机操作序列（约 1/3 打包进 batch 形成多 info 重放）后逐节点与朴素 JS 全量重算比对；同轮引入生产语义 CI 差分（`pnpm test:prod`，`__DEV__:false` 编译源码跑同套测试，dev 特化文件排除清单见 `vitest.prod.config.ts`）。首跑均未发现反例。资产：`__tests__/modelComparisonFuzz.spec.ts`（同时是 RxSet 运算族/RxMap 派生/createSelection 的 batchReplay 列与链式管道的对账资产）、`vitest.prod.config.ts` + CI 步骤。
  12. NaN/-0 元素值域差分（weirdNum 列）+ 核心表面账本化 + patch 错误恢复探针。动态复现并修复三个缺陷类：toSorted 增量删除按 `===`/`indexOf` 定位在 NaN（找不到→静默残留）与 -0/0（tie 组内命中错误实例）下与全量分叉——修复为 `Object.is` 身份定位 + 定位失败/tie 组含可区分成员时回退全量；applyPatch 抛错后 phase 停留在 PATCH_PHASE、抛错轮 info 已消费——下次触发只增量重放新 info，抛错轮变更永久缺失——修复为 handleRecomputeError 统一回退 FULL_RECOMPUTE_PHASE；findIndex 的 splice 分支用终态长度回推"操作时长度"归一化负/越界 start，多 info 重放下回推失效、被删除的旧 match 静默保留（fuzzKit 统一对抗参数域后由 batchReplayFuzz 命中）——修复为多 info 一律回退全量（等价类同 groupBy/slice，README 脚注已更新）。契约裁定：toSorted comparator 必须构成一致全序（NaN × 裸数值 comparator 属契约外），README 参数契约节已更新。资产：`__tests__/weirdNumbersFuzz.spec.ts`（NaN/-0 值域差分）、`__tests__/coreLifecycleGaps.spec.ts`（async/generator getter × destroy、排队后 destroy、三参调度器契约、调度错误兜底、patch 错误恢复、oncePromise 拒绝、object-atom 协议特征）、`__tests__/coreSurfaceInventory.spec.ts`（核心表面账本 + 导出面普查）、`__tests__/lifecycleAndReplayFixes.spec.ts`（findIndex 多 info 最小回归）。
  13. 消费者契约回放（consumer contract pinning）：读取 axii/axle 宿主（RxListHost）对 triggerInfo 的真实消费面（splice 的 argv 原始透传/methodResult 删除项、reorder 的 argv[0]+reorderInfo 结构、EXPLICIT_KEY_CHANGE 的 key 透传与 methodResult 旧值、RxSet add/delete 的 argv 与 replace 的 [newItems,deletedItems]、RxMap set/delete/clear/replace 的 methodResult 形状），在 data0 侧钉成协议形状测试——改动字段形状在本仓 CI 当场失败，而不是等下游升级才发现断链。**反向回放（下游侧，2026-H2 完成）**：以 sibling checkout 在 axii/axle 各自仓内对 2.9 语义跑全套 + 契约测试。发现并修复 axii 真实缺陷：本仓缺陷类 4 的修复（patch 抛错后重跑 computation）打破了 axii RxListHost「computation 只跑一次」假设——重跑时残留 hosts 令行重复创建、撞 "should never rerender" 断言，列表区域永久崩坏（axii#37 修复 + 双代契约资产）；axle 因 applyPatch 全部就地消化错误、永不上抛而构造性免疫，该前提已在 axle#25 钉成契约。教训：**行为语义（非字段形状）的变更同样需要双向契约资产**，axii 的 data0Contract.spec 条款 8 与本仓的错误恢复语义现在互相锁定。同轮账本燃尽：核心账本 13 格清零（generator patch 全维、generator getter 错误/交错/batch、asyncGetter batch、objectProxy batch、onChange 错误/batch、children batch、probe 错误注入），集合账本关闭 undefined × RxMap/RxSet/selection、weirdNum × selection、batchReplay × reduce/indexBy/toMap/selection、destroy × toMap/createSelections/RxSet.has/size/isSupersetOf；RxTime 修复多入口 disposer 覆盖泄漏（resolve+subscribe 混用时先注册的 autorun 永久泄漏，第一轮静态确认的遗留缺陷）并以 fake-timer 确定性审计取代真实时钟依赖。资产：`__tests__/consumerContractReplay.spec.ts`、`__tests__/coreLedgerBurndown.spec.ts`、`__tests__/collectionLedgerBurndown.spec.ts`、`__tests__/rxTimeDeterministic.spec.ts`。
  14. 增量性见证（incrementality witness）+ 债务清仓轮：`fullRecompute` 见证事件把「增量 ≠ 静默回退」变成可断言语义，README 支持矩阵首次获得双向可执行定义（增量格子零回退 + 重算格子必回退），直接猎杀 mutation 审计暴露的最大幸存类；同轮修复缺陷类 7（toMap/属性形式 indexBy × OOB set：`undefined` 行解构/属性读 TypeError 且派生链永久毒化，违反稀疏 sweep 等价类——修复为洞位行统一跳过，全量与 patch 两侧一致）；集合账本 33 格清零（weirdNum × RxSet 代数差分 fuzz、undefined/NaN × indexBy/toMap/reduce/selections/RxSet 谓词、sparseOOB × toMap/reduce/createSelections、batch × RxSet 谓词）；外围模块补齐（AsyncRxSlice 的 receipt 交错/错误路径/销毁、LinkedList 的结构操作/记账清理/响应式迭代）。资产：`__tests__/incrementalityWitness.spec.ts`、`__tests__/collectionLedgerBurndown2.spec.ts`、`__tests__/peripheralModules.spec.ts`。
  15. 幸存 mutant 语料驱动的语义钉扎（survivor-corpus pinning）+ 死代码清偿：把 PR #9 显式在账的"439+146 幸存 mutant 渐进消化"作为立项对象，重跑两模块审计后**逐 mutant 分类**幸存语料，区分「安全方向变异」（回退强制/范围放宽/快慢路径选择/防御分支——结果仍正确，构造性不可杀，见下方基线记录的等价类裁定）与真实检出盲区，只对后者补资产：契约守卫可达性（reposition/swap 越界、indexBy/toMap 重复 key、destroyed clear/set/reorder no-op）、reorderInfo 协议**字段值**钉扎（consumerContractReplay 只钉过形状）、at(index) 精确触发的有界穷举（触发不多/不少/升序——差分 fuzz 只对比终值，从不检查"谁被通知了"）、slice 区间算术穷举 + 增量性边界（start=0 窗口、负边界 set 不回退）、groupBy × NaN key、filter/slice 幽灵触发钉扎、async 打断的 asyncStatus 转换序、generator 过期轮段级丢弃、recompute(force) 越过 PATCH 阶段、markDirty 传播、updatedAt 惰性同步、dev 不变量开火自检扩展（atomIndexes 漂移/越长、行 frame 错位）。同轮死代码清偿：findIndex 的逐项增量 cache（被 hasItemReactiveDeps 全量回退门构造性遮蔽，36 个 no-cov mutant 的来源，热路径每步搜索白付一次对象分配）、Computed 的 keyToEffectFrames/hasDeps/getCachedValue/实例 collectEffect、util 的 uuid/isDate/isRegExp/getStackTrace/hasOwn/isString/isSymbol/isObject/isFunction/isPromise/toRawType（全部零生产引用，仅覆盖率测试喂养）。资产：`__tests__/mutationKillersRxList.spec.ts`、`__tests__/mutationKillersComputed.spec.ts`、`__tests__/invariantAssertions.spec.ts`（扩展）。
  16. 宿主语义差分回放（host-semantic differential replay）+ 核心平面重入边界：按 axii/axle RxListHost 真实消费面维护平行 `hosts[]`，断言每步（含 batch 多 info、派生链作 hosted source、回调重入、digest 内 batch、嵌套 batch、中途 destroy sibling）`hosts ≡ source.data`。对照"axii 现状"（EKC 读 `source.data[key]`）与"协议正确"（`info.newValue`）：前者在 batch `set`+结构操作下动态分叉，后者全程对齐——协议字段充足，分叉属下游消费脚枪。同轮动态复现并修复：`isPlainObject` 用 `constructor` 门控导致 `Object.create(null)`/`constructor` 覆写后 object-atom 属性读失明（改原型链判定）；AsyncRxSlice destroy 不抬升 receipt 致在途 reject 僵尸写 `loadError`（destroyResources bump receipt + 复位控制 atom）。嵌套 batch/digest 内 batch、mid-recompute create/destroy、RxTime 多入口清理未发现反例。资产：`__tests__/deepReview2026H3Findings.spec.ts`、`__tests__/_coreReview16*.spec.ts`；README「RxList 参数契约」补 EKC 须消费 `info.newValue`。
  17. 身份别名差分（identity-aliasing differential）：同一对象引用出现在 RxList/RxMap/RxSet/LinkedList 多位置，经一路径变更后与全量重算/原生结构对账。动态复现并修复 `groupBy` 空组键残留（增量 remove/set 清空组后只清内容不删 map 键，`has`/`size`/`keys` 与全量分叉；既有 fuzz 只断言已有键的内容，空≡空通过）。同轮：LinkedList 重复身份下 `removeBetween` 误删幸存者 WeakMap 条目（仅当映射指向本节点才删）；`spliceMany` 大数组路径 × `NaN` deleteCount 与原生分叉但当前公开面不可达。别名 × toSet/filter/map/selection/RxMap 未发现反例。资产：`__tests__/deepReview2026H3Findings.spec.ts`；fuzz 族补 groupBy **键集**断言（broad/duplicate/batchReplay/weirdNumbers/modelComparison）。
  18. 协议命名空间碰撞审计 + 真实 GC 可达性/记账残留审计（2026-H3 第二轮）。(a) 把「用户数据值域 ∩ 内部协议常量」当对抗值域：METHOD/EXPLICIT_KEY_CHANGE 订阅曾以字符串枚举值直接作 depsMap key，RxMap key / RxSet 成员 / groupBy 组键 / indexBy·toMap 键恰为 `'method'`/`'explicit_key_change'`（HTTP method 分组是现实输入）时，SET/ADD/DELETE 的 key dep 与协议订阅者同 dep——RxMap.keys 的 assert(unreachable) 抛给 set/delete 调用方、RxSet.toList/代数族解构 TypeError 且静默分叉。修复为 track/trigger 双侧 Symbol 隔离（`METHOD_TRACK_KEY`/`EXPLICIT_KEY_CHANGE_TRACK_KEY`，公开 manualTrack 调用形状不变，构造性关死等价类）。同轴：trigger 的 `key !== void 0` 把「key 恰为 undefined」当「未提供 key」，`RxMap.get(undefined)` 订阅者永久漏触发（改 `'key' in inputInfo`）；`RxMap.set` 的 `===` 判等与库内 Object.is 身份语义分叉（0→-0 已写入却不触发、NaN→NaN 重复触发）。(b) WeakRef × `--expose-gc` 的真实堆审计（retainedDiagnostics 是计数器、measure-retained 是称重，都不证 GC 可达性）：destroy 后派生结构全部可回收 ✓；但「订阅不同 key → 退订」churn 在长活 source 的 depsMap 留下无界空 Dep 条目（10000 次 `at(i)`/`get(key)` 循环 → 10000 条残留）——修复为 dep 记录 host/hostKey、销毁路径退订到空即摘除（cleanup 仅 `!active` 时摘：活跃重算的复位路径零开销，findIndex patch 曾因无差别摘除 -13%），async asyncTracks 重放与 restoreEffectDeps 两条重添加路径挂回宿主或并入现行 dep，`pruneIndexKeyDeps` 与 depsMap 重同步；审计还暴露 destroy 后完成的 async/generator getter 把僵尸 effect 重订阅回 dep（asyncTracks 重放无 active 门，泄漏 + 幽灵调度）。(c) 顺带（操作时位置 × 重放终态的账内清偿）：reduce/reduceToAtom 的「纯尾插」判定按终态长度回推，多 info 下「越界 clamp 尾插」误入增量且 index 分叉——判定与应用改走 digestReplay 操作时长度，batch 连续 push 由此保持增量（README 脚注已更新）。热路径核验：定向 ABBA 微基准（15 轮取中位）write-atom +0.1%、findIndex patch −0.6%、map patch +1.4%、at() −2.5%，均在环境噪声内（vitest bench 的 main-vs-main 噪声中位 3.5%、最大 16.6%，不足以裁定本级别改动）。资产：`__tests__/deepReview2026H3Round2.spec.ts`（六个缺陷类的实例回归 + 等价类横扫）、`scripts/audit-reachability.mjs`（可回收性 + 记账有界性，周期性运行，不进 CI）。

  19. 创建时形态假设 × 运行时形态迁移横扫（shape-migration sweep，2026-H3 第三轮）+ notify.ts/RxSet.ts 首轮 mutation 审计。(a) 攻击轴：多个结构的运行时「形态」在创建时一次性定型——atom 的 primitive/proxy 形态由 initValue 决定、派生列表的段偏移/index 校正区间算术假设源保持稠密、RxTime 的定时器排定假设线性表达式非退化；让值/形态在运行时迁移穿过这些创建时假设（primitive atom 写入对象、稠密列表经 OOB set 变稀疏后走校正/分段路径、RxTime 系数相消）。既有稀疏 sweep 只覆盖「稀疏 × 纯尾插」——尾插不进任何按区间遍历的校正路径，盲格在「稀疏 × 不等长 splice / reorder / EKC」。动态复现并修复四个缺陷类：createIndexKeySelection 两条 index 校正循环（不等长 splice 平移区、reorder affectedRange）撞洞位行 TypeError 直接抛给 list.splice/swap 调用方——违反「OOB set × 派生算子不崩溃且可恢复」等价类，修复为 ?. 跳洞（与 map/filter 行级守卫一致）；concat 的 EKC 分支对越界 key（段长跳变）按段内偏移直写，跨段覆盖后续源的段（B 段整体错位，结构性错乱而非洞物化差异）——修复为 key ≥ 旧段长（本列表长 − 其他源现长）回退全量；RxMap.replace 的 SET 触发缺 Object.is 判等门与 oldValue 字段——方法 18 修 set() 的同一等价类漏网（**判等门必须覆盖同一变更语义的所有入口**），整表 replace 幽灵触发全部 get(key) 订阅者；RxTime 系数相消（coefficient=0）时 -constant/0=±Infinity → setTimeout(Infinity)，Node 打 TimeoutOverflowWarning 并 clamp 成 1ms 虚假唤醒——修复为 isFinite 守卫。特征钉扎（契约边界，README「传播模型」已同步）：atom 对象特性由创建时 initValue 形态定型，primitive→object 值迁移后属性级读写静默落在 atom 函数对象上（不写 value、不触发；axii atom(null) 惯用法的整值替换不受影响）；class 实例 atom 属性写（写穿实例+触发）与属性读（不转发）不对称。稀疏残留分叉（groupBy reorder 分支 filter 跳洞 vs 全量物化洞、toSet 洞成员物化差）经评估归入「洞的物化语义」既有契约外债务，不崩溃可恢复，未立案。(b) notify.ts 首轮 mutation 审计（见基线记录）＋死代码清偿：triggerEffects 的 CompactDep 快路径为不可达死分支（CompactDep 只挂在 primitive atom 函数对象上、派发一律走 triggerPrimitiveAtomValue 特化路径，从不进 targetMap），删除；真实检出盲区补杀 3 个（triggerPrimitiveAtomValue 的 shouldTrigger 暂停门、scheduleAtomEffect 的 session info 形状与逐次累积、多 dep 去重的恰一次派发）。(c) 下游反向确认：axii（sibling alias，53 文件 672 tests）、axle（临时 alias data0→HEAD + __DEV__ define，33 文件 459 tests）对修复后 HEAD 全绿；热路径核验：triggerEffects 死分支删除经定向 ABBA 微基准（object-atom 写 ×200k + splice ×30k，15 轮取中位）−0.99%，噪声内。资产：`__tests__/deepReview2026H3Round3.spec.ts`（四缺陷类实例回归 + 形态迁移特征钉扎 + notify 盲区补杀）、`__tests__/sparseSetOperatorsSweep.spec.ts` 校正路径横扫（不等长 splice + swap × 全算子族，洞位行等价类）。(d) **根因机械化轮**（同轮追加，五项结构性补法全部落地为常驻资产）：① 形态操作升格为 fuzzKit 生成器（`performRandomListOp`/`adversarialSetIndex`），`sparseOpsFuzz.spec.ts` 以二级 oracle（T1 不崩溃 / T2 强制全量重算后 ≡ 逐算子洞位语义参考实现 / T3 恢复后增量自洽）让契约外输入进入组合搜索——首跑即命中三个新缺陷类：**R3-5 幽灵 EKC**（负/小数 key 的 set 是数组属性赋值、不触及元素，但 filter 的 pending 机制把它物化成真实行插入、slice 会替换掉区间内真实行、groupBy/toSet/indexBy/toMap/selection 会加幽灵成员——修复为 `isDenseIndexKey` 守卫统一忽略，协议侧 key 仍原样透传；toSorted 的 reorder 因 key 是 ITERATE_KEY Symbol 险被同一守卫吞掉，显式化 reorder→回退分支并钉 R3-8，README 矩阵 toSorted×reorder 格子勘误为「重算(tie 稳定序随源序)」）；**R3-6 filter × mapList 全量重算不重建**（错误恢复/多 info 回退走全量时旧 filtered 内容滞留、pending 引流进死通道——修复为 fullRecompute 见证事件里复位增量状态并整体重建）；**R3-7 groupBy reorder 分支 Array#filter 跳洞**（已物化的 undefined 组经 reorder 被清空,与全量分叉——修复为全下标扫描,洞位语义两侧一致）。①′ 同轴顺带（GC 审计扩展命中）：**R3-9 selection itemToIndicators 记账无界**——createSelectionInner 的 computation 每次全量重算为每行 createNewIndicator，旧行条目无人回收（force recompute/错误恢复路径下 Map 值升级 Set 后无界累积 + currentValues 每次变化遍历写全部死 indicator），违反方法 18 的"谁建记账条目谁给回收路径"规则——修复为 computation 全量重建时 resetIndicators() 清账（行级增删仍走 deleteIndicator 精确回收）；audit-reachability.mjs 新增 selection churn 的 WeakRef 检查（修复前 FAIL）。② 触发精确度成为常驻观察列（`fuzzKit.attachRecomputeCounter` + `triggerPrecisionFuzz.spec.ts`：digest 有界性/无关源隔离/判等门零触发）。③ 变更语义 × 入口账本（`surfaceClassification.ts` 单一事实源 + `entryPointSemanticsInventory.spec.ts`：MUTATIONS 全员归组机械强制,equalityGatedWrite 组全入口继承 Object.is 探针,RxList.set 显式豁免+特征钉扎）。④ mutation 闭合判据 + 全模块首审清零（见基线记录）。⑤ 性能优化行为特征差分规则 + `creationShapeMatrix.spec.ts`（atom 形态选择 × 能力矩阵整表钉死）。
  20. 行级依赖触发序 × 结构 info 派发序交错审计（非 batch 路径）+ patch 插入侧值域守卫不对称横扫（2026-H3 第四轮）。(a) 攻击轴：既有交错方法（4/7/16）都以 batch/调度器为载体，隐含"行级依赖与结构 info 的相对序只在 session 队列里才成问题"；本轮审计**非 batch 的同步内联触发序**——变更方法内部"先写行级可见的 atom、后派发结构 info"的窗口里，行级订阅者被原子写同步执行，此刻一切 pending-info 守卫都看不到任何东西。动态复现 **R4-1**：`reorder` 在派发 METHOD info 前逐个写 index atom，mapFn 读取 `index()` 的行级 rowComputed 被同步触发，其 hasPendingStructuralInfos 守卫失效、按终态位置直写派生列表，随后派生列表的 reorder patch 又按 order 搬移一次——**双重搬移，silent 乱序**（`swap/sortSelf/reposition` × `map((item,index)=>… index() …)` 全中，batch 路径因 info 先入队而幸免）。修复为 reorder 与 doSplice 的 session 顺序对齐：结构 info 先入队、index atom 值写入进同一 session，digest 时结构 patch 先应用。既有 fuzz 盲区形态：所有 map(item,index) 生成器只把 index atom **存进**结果（`{item, index}`），从不在 mapFn 里**读** `index()`，行从不升级为带 index 依赖的 rowComputed——"传了 index 参数"与"读了 index 值"是两个覆盖等价类。(b) patch 插入侧与全量 computation 的行值域守卫不对称：indexBy/toMap 的全量侧与 patch **删除侧**都跳过 null/undefined 行，**插入侧**（splice 新增项、EKC 的 newValue，indexBy 的 EKC oldValue 亦漏 null）直接属性读/解构——动态复现 **R4-2**：`push(null)`/`push(undefined)`/`set(i, undefined)` 当场 TypeError 抛给变更调用方。修复为插入/替换双侧补齐与全量一致的守卫；等价类同 §3.1 第 3 问（守卫语义 = 行值域 × entry 存在性，入口 = 全量/patch 删除/patch 插入/EKC 双侧）。(c) 顺带（"patch 端 spread 不定长数组进函数调用"账内清偿）：**R4-3** slice patch 的 ucHead+ucTail 中间段替换以 spread 传参，中间段与源 splice 插入量同量级（十万行 replaceData 恰是 spliceMany 的设计场景）——150k 中段插入直接 `Maximum call stack size exceeded` 抛给变更调用方；修复为 spliceArray（源内 spread 全扫后其余调用点均有界：push/unshift 的 spread 量 = 用户自身实参量）。资产：`__tests__/deepReview2026H3Round4.spec.ts`（三缺陷类实例回归 + 读 index 行 × 全操作域差分 fuzz + null/undefined × indexBy/toMap 全入口横扫 + 150k 中段 slice 回归），coverageInventory 对应格子已过账。下游反向确认：axii（sibling alias，53 文件 672 tests）、axle（临时 alias data0→HEAD，33 文件 459 tests）对修复后 HEAD 全绿；热路径核验：reorder 定向 ABBA 微基准（plain 2k×200 / withIndex 500×100 / swapHeavy 50×20k，15 轮取中位）+0.8%/+2.7%/+2.9%，参照方法 18 记录的环境噪声界（中位 3.5%）判为噪声内。(d) **补法机械化轮**（同轮追加，反思归因的六项补法全部落地为常驻资产）：① 行形态生成维度进 fuzzKit（`indexReadingMapFn`/`valueStoringMapFn`/`itemAtomReadingMapFn` 族），broadOperatorsFuzz 的 map 列并行 storesValue/readsIndex 双形态 + readsItemAtom × batch 混排（§3.3 新规则）；② 值域 × 进入通道账本（`VALUE_INGRESS_INVENTORY` 五通道过账 + `valueIngressChannels.spec.ts` 九算子横扫，§3.3 新规则）；③ 大 N 冒烟层（`largeScaleSmoke.spec.ts`，10^5 量级 × 全算子族一发关键操作，进 CI 约 2.6s；顺带登记规模债务：groupBy/toSorted 批量增量插入 O(k×n)，见 §3.3）；④ 不变量升格规则（§4 新条目）+ 首批三项登记：`dispatchStructuralThen` 构造性原语（doSplice/reorder 的 info-先于-原子写唯一出口；ABBA 五列 plain/withIndex/swapHeavy/spliceIdx/splicePlain = +1.4%/−0.3%/+0.7%/−0.7%/−1.7%，噪声内）、`sourceInvariants.spec.ts` 静态执法（spread 进调用的允许清单 + 扫描器自检）、valueIngress 行为执法；⑤ mutation 幸存分类第 (e) 类「套件无法构造触发态」+ 守卫 load-bearing 场景三例 killer（变异自证通过）；⑥ 下游反馈标记（`DOWNSTREAM_FEEDBACK`，grep 事实：33/33 算子零下游反馈，coverageInventory 机械强制全员标注）。已知未修（`test.fails` 在案，knownIssuesReproductions）：RxTime.eq 越点不可观察为 true（+2ms 唤醒 skew × 点相等谓词，定时器的存在证明越点可观察是实现意图）——修复需先裁定语义（脉冲/容差窗口/移除），gt/lt 不受影响。(e) **规模债务清偿**（同轮追加）：groupBy 批量 splice 改单遍分桶（结构事实：同一变更块的同 key 项在组内必然连续 ⇒ 每组恰 ≤1 次删除 + ≤1 次插入 spliceArray，位置 = 前缀同 key 计数，前缀只扫一遍；单项操作保留零分配快路径），reorder 分支改一次 O(n) 分桶（曾每组全扫 = O(组数×n)）；toSorted 批量超阈值回退全量（bulk > 64 且「bulk×4 > m 或 bulk > 4096」，交叉点实测 m=1k/10k/100k 时 k*≈200/2400/5300，无订阅者下界）。实测（3 轮中位）：groupBy × replaceData 20k/100k = 283ms→2.7ms / **6.59s→10.8ms**（105×/610×），groupBy reorder 20k 行 2k 组 = 130ms→6.3ms（21×），toSorted × replaceData 20k/100k = 47ms→3.6ms / **2.03s→13.7ms**（13×/148×）；单项快路径 ABBA（干净进程 15 轮中位）groupBy push/delete、toSorted 单项 = +2.3%/−0.5%/−2.0%，噪声内。行为语义：groupBy 幸存组引用稳定不变、组集不变，**每组触发从 k 条收敛到 ≤2 条 splice**（形状钉扎入 round4 资产）；toSorted 回退加入 tie/undefined 既有家族（README 矩阵与脚注已同步）。资产：deepReview2026H3Round4 的批量差分 fuzz（seed 71-73，NaN key × 快慢路径交替）+ 引用稳定/触发形状/阈值双门/batch 混排回归，largeScaleSmoke 的 10^5 批量格子。
  21. 结构级自别名差分 + 下标数值上界域（2026-H3 第五轮）。(a) 攻击轴：方法 17 的身份别名把**元素**放进多个位置，本轮把 **Rx 结构本身**放进一个派生的多个操作数位置（`a.concat(a)` 跑马灯复制、`s.union(s)`、`b.concat(a, a)`）。动态复现 **R5-1**：concat 的 applyPatch 以 `sources.indexOf(info.source)` 定位段——同一源占多段时恒命中首段，其余段静默漏 patch（增量 `[1,2,3,1,2]` vs 全量 `[1,2,3,1,2,3]`，batch 与非 batch 同中）；修复为构造期检测重复源、patch 恒回退全量（位置增量对多段无法同时表达，逐段应用会互相移动 offset；README 脚注已同步）。RxSet 代数族（union/difference/symmetricDifference/intersection × add/delete/replace × batch）按终态成员判定，自别名下未发现反例——构造性安全，已钉特征测试。(b) 攻击轴：EKC key 的数值上界。合法数组下标域是 [0, 2^32-2]，`isDenseIndexKey` 曾只挡负/小数——key ≥ 2^32-1 的正整数（`data[k]=v` 是属性赋值：length 不变、全量 computation 不可见）穿透守卫。动态复现 **R5-2** 三个形态：filter 物化幽灵行（[2,4]→[2,4,6]）、groupBy 物化幽灵成员且 insertInSourceOrder 前缀循环按 key 迭代 ~2^32 次（单次 `set(2^32+5)` 实测卡 60s，DoS 级）、`ensureAtomIndex` 把 atomIndexes.length 撑到 ≥ 2^32 当场 RangeError 抛给 `set()` 调用方且 **data 已写、info 未发**（状态撕裂，破坏"OOB set 不崩溃且可恢复"等价类）。修复为 `isDenseIndexKey` 补上界（单点谓词，全部 8 个消费者继承）、`set()` 的 atomIndexes 门与 concat 的内联 key 检查统一走该谓词、groupBy 插入侧前缀循环补 `Math.min(sourceIndex, stateNow.length)` clamp（与 removeAtSourcePosition 对称，兄弟实现点一致性）；上界邻域值（2^32-1 + [0,3)）进入 `fuzzKit.adversarialSetIndex` 生成器常驻组合空间（§3.3 生成器规则，非新开 sweep）。协议侧 key 仍原样透传（round5 资产钉扎）。下游反向确认：axii（sibling alias 53 文件 672 tests）、axle（临时 alias data0→HEAD，33 文件 459 tests）对修复后 HEAD 全绿。资产：`__tests__/deepReview2026H3Round5.spec.ts`（两缺陷类实例回归 + RxSet 自别名特征钉扎 + 上界 × 派生家族横扫 + 协议透传钉扎）、`fuzzKit.adversarialSetIndex` 上界值域。(c) **盲区假设清偿轮**（同轮追加，把反思产出的未探测清单逐项动态验证）：① **协议载荷所有权（输出侧别名，动态复现,待裁定）**——`splice`/`clear` 等的返回数组与 `info.methodResult` 同引用（`RxSet.replace` 的 `[newItems,deletedItems]` 同形），非 batch 下同步消费不可见，batch/async applyPatch 跨 await/onChange handler 直接持有 info/自定义调度器 infos 参数四个入口把消费推迟——调用方按原生 splice 预期改写返回数组即静默毒化全部 patch 消费者与 digestReplay 重建（batch 内 `removed.length=0` → groupBy 删除全丢；onChange 先订阅时 handler 改 methodResult 同样毒化后订阅的 groupBy，无需 batch）。A3 只裁定了输入侧采纳，输出侧从未裁定；方向（延迟场景防御拷贝 vs README 钉"返回数组 digest 前只读"契约）待维护者定，`test.fails` 在案（knownIssuesReproductions）。② **不稳定 getKey（动态复现,契约缺口）**——groupBy 的 getKey 每次返回新引用时增量组维护按引用定位必然失联（incSize 2 vs full 1），与 toSorted comparator 一致性契约同族：分组/索引回调必须纯且稳定，README 待与 D3（回调读响应式数据静默陈旧：groupBy/toSorted/reduce/indexBy 与 find/filter/map 家族不对称）一并裁定后成文。③ **自引用回调（未发现反例,已钉扎）**——谓词/mapFn 经 `at(0)` 读列表自身（"大于首元素"是现实需求），set(0)/头部 splice/swap/batch 混排下 filter/map 全收敛，收敛特征钉入 round5 资产（R5-3；此前没有任何测试构造过自引用回调形态）。④ **deleteCount 对抗域进生成器**——各生成器的 deleteCount 曾恒为良性 0..3（与"越界代表值只有 len+1..3"同一种分域窄化），`fuzzKit.adversarialSpliceDeleteCount`（负/NaN/Infinity/越界/小数）落地并接入 performRandomListOp/broadOperatorsFuzz/batchReplayFuzz，全 fuzz 重跑无反例。⑤ **混合类型值域（部分钉扎）**——对象引用作 groupBy 组键（引用身份增量 ≡ 全量,R5-4 钉扎）、冻结元素 × map/filter/toSorted、getter 元素 × indexBy 属性形式、对象作 RxMap key 均未发现反例。⑥ **atom proxy 元键/自省边界（特征钉扎）**——值属性名与元 API（`raw`/`call`）同名时读被遮蔽写落值对象（读写不对称）、`in`/`Object.keys` 不转发到值对象,钉入 creationShapeMatrix,README「传播模型」已记录。⑦ **-0 作 set key（未发现反例,已钉扎）**——SameValueZero 记账 + ToString 归一使 set(-0) ≡ set(0) 全链一致（R5-4）。(d) **裁定执行轮**（同轮追加，三项挂起裁定按维护者决定全部落地）：**D1 载荷所有权 = 防御拷贝**——`util.toProtocolPayload` 使 methodResult/reorder argv[0]/RxSet.replace 内层数组持独立副本（返回数组归调用方，原生 splice 预期；空载荷共享冻结单例，纯尾插热路径零额外分配）；观察出口（onChange handler、自定义调度器 infos 参数）经 `computed.copyTriggerInfoPayload` 发一层深副本；直接实现 applyPatch 的协议消费者仍拿共享引用、契约只读（逐 patch 拷贝落热路径）。**执法方式经 ABBA 回退一次**：曾在 Computed.trigger 汇聚点对 argv/methodResult 做 dev Object.freeze，ABBA 实测 dev push +20.0%/单删 splice +28.7%/swapHeavy +53.7%（freeze 调用本身 + 冻结数组的 elements-kind 转换拖慢一切后续消费者），弃冻结改为消费边界拷贝；终版 ABBA 三轮取中位 push +0.4%/单删 splice +1.0%/replaceData20k +3.0%/swapHeavy −2.4%，均在方法 18 记录的噪声界（中位 3.5%）内。knownIssues 的 test.fails 转普通回归。**D2 = 规范下标字符串入口归一化**——`normalizeIndexKey` 在 set/at 入口把平台规范下标字符串（`String(Number(k))===k` 且 [0,2^32-1)）转 number + dev 警告（`data["2"]=v` 平台语义就是写第 2 行，守卫域窄于平台域时源已变而派生/at 订阅者静默失联——R5-2 数值上界的镜像方向）；非规范字符串（"02"/"2.5"/"-0"）保持属性语义。**D3 = 回调纯度契约 + dev 探测**——README 参数契约成文（groupBy/indexBy getKey、toSorted comparator、reduceFn 必须纯且确定，响应式读不建依赖；find/findIndex/map/filter 家族支持响应式回调）；dev 下以 detached `MapItemDependencyProbe` 对首元素探测一次（捕获即弃，零残留订阅经触发计数钉扎），响应式读与不稳定键（SameValueZero 双调用比对）均告警；reduceFn 不探测（双调用会向累加器重复写入、probe 会劫持其子 effect 父子归属），纯度由契约承载。资产：round5 文件 R5-D1/D2/D3 组（含 async patch 窗口回归、平台下标域 13 键「set 后增量 ≡ 全量」性质横扫、探测零残留钉扎）；下游 axii/axle 对裁定后 HEAD 复跑全绿；RxList.ts mutation 复审见基线记录。

  22. 判等语义跨模式差分 + 实例缓存惰性结构的创建作用域审计 + 错误遮蔽路径审计（2026-H3 第六轮；评估与修复分两步落地——先 test.fails 钉证据，裁定后同轮修复）。(a) 攻击轴：同一 API 的**双模式实现**（createSelection 的 atom 单选 vs RxSet 多选）之间做判等语义差分——记账 Map（itemToIndicators，SameValueZero）与 RxSet 分支（Set.has，SameValueZero）一致，而 atom 分支三个入口（createNewIndicator、autoReset 的 deleteCurrentValueIfItemRemoved、全量重建）用 `===`。动态复现 **R6-1**：NaN item × atom 单选下「增量 indicator 置 true（Map.get 命中）vs 全量重建恒 false（=== 不命中）」分叉 + autoResetValue 不回收已删除的 NaN 选中值（RxSet 模式可回收）。方法 18/19 的「判等门覆盖同一语义所有入口」曾沿"变更方法入口"轴过账，本轮补的是"同一 API 的并行实现模式"轴；coverageInventory 的 createSelection × weirdNum 格子只点亮了 RxSet 路径（§3.3 值域 × 入口教训的实例）。(b) 攻击轴：**实例缓存的惰性副作用结构 × 创建作用域生命周期**——RxList.length/RxMap.keys/size 都用 createDetached 隔离（历史缺陷的修复），横扫其余"实例字段缓存 + 惰性创建"的结构。动态复现 **R6-2**：AsyncRxSlice.fetch() 的 autoFetchPromise 无 createDetached——首次 fetch 在 autorun/computed 内发生时被收集为宿主 child，宿主重算即销毁，此后响应式参数变化不再重新拉取、fetch() 永远返回旧 promise（静默陈旧）。RxTime.resolve 的内部 autorun 同属此等价类（未单独钉扎）。(c) 错误遮蔽：batch fn 抛错 × 订阅者也抛错时，finally 中 digest 重抛 firstError 会按 JS finally-throw 语义**静默替换** body 异常（无 console 兜底，动态复现 **R6-3**）；README §3 未裁定此优先级。(d) 接口谎言：atom.fixed/atom.lazy 经 IS_ATOM 通过 isAtom 却无 `.raw`——AtomComputed.replaceData 的解包路径把 computed(() => atom.fixed(42)) 静默变成 undefined（动态复现 **R6-4**）。四项先以 `test.fails` 钉入 `knownIssuesReproductions.spec.ts`,裁定后同轮修复并全部翻转为普通回归：**R6-1** = atom 分支三入口统一 `sameValueZero`（createNewIndicator/deleteCurrentValueIfItemRemoved,与记账 Map/RxSet 侧对齐）；**R6-2** = AsyncRxSlice.autoFetchPromise 与 RxTime.resolve 的内部 autorun 改经 `ReactiveEffect.createDetached` 创建（生命周期归实例,与 length/keys/size 先例一致）；**R6-3** 裁定 = body 异常优先——batch 体抛错时 digest 照常执行（其余订阅者不受牵连）,订阅者错误降级 console.error,body 异常照常传播（README §3 已成文）；**R6-4** 裁定 = 补全接口——fixed/lazy 补 `raw` 访问器（读值不追踪,lazy 经 pauseTracking 隔离）。等价类资产：`__tests__/deepReview2026H3Round6.spec.ts`（R6-1 atom 单选全入口 × NaN/-0 横扫含 createSelections 多列继承、R6-2 实例缓存惰性结构全家族 × 宿主重算横扫、R6-3 body × 订阅者错误四象限矩阵含嵌套 batch、R6-4 isAtom ⇒ AtomBase 契约三面）；coverageInventory 的 createSelection/createSelections × weirdNum 格子已补 atom 单选资产。未发现反例的探测面：RxMap.keys/values/entries × batch delete+set 重排序、RxMap.replace × get(key) 订阅者、RxSet.replace 自别名、reduce × batch 尾插、at() churn 无写入下 indexKeyDeps 有界性（≤ 列表长）、map(读 index) × 兄弟 map 销毁后 unshift。验证基线（修复后）：全套 + prod 语义 + type-check + build 全绿,axii/axle sibling 反向确认全绿。(b) **工程面清偿（同轮追加，review 报告的改进项全部落地）**：① dev/prod 双构建 + `development` 条件导出——npm 消费者的开发模式从此获得纯度探测/销毁警告/不变量断言（此前 dist 只出 `__DEV__:false`，21 轮建的 dev 防线从未触达真实用户）；`require` 条件补 `.d.cts`（node16 解析实测通过）、`main`→umd.cjs/`module`→esm 对齐、`sideEffects:false`；Node 四条件（default/development × esm/cjs）实测行为差分正确。② release.js 补 `git push --follow-tags` + `--tags`（静态确认的缺陷：v2.10.0-v2.12.0 远端无 tag）,fake-git 参数断言入 knownIssuesReproductions。③ async generator getter/applyPatch loud-fail（曾静默变成从未推进的 AsyncGenerator 值/被当 patch 成功；README「async 契约」成文,含转译降级契约外说明）;isAsync/isGenerator 补 `?.` 防 null 原型函数。④ RxList.ts 拆分第一步：selection 家族（~435 行）移入 `src/rxListSelection.ts`（自然模块边界:独立函数非类方法）,RxList.ts 原位 re-export,公开导入路径零变化;共享守卫 isDenseIndexKey/sameValueZero 以 @internal 导出保持单一定义;循环依赖与 RxList↔RxSet 同款安全前提（双方只在函数体内使用对方）。⑤ 死代码/死面清偿：patchRecompute 的 'recomputeDeps' 死事件删除（全仓零监听者）、RxMap 构造器 forceAtom 死参数删除、RxList/RxMap/RxSet 构造器参数属性改条件赋值（与 Computed 的"有值才赋"优化对齐,groupBy 每组一个 RxList 的场景不再白付 undefined 槽位）、冗余 `this.getter = getter` 删除、toMap 断言消息勘误（曾复制 indexBy 文案）、TriggerOpTypes.CLEAR 裁定保留（手动 notifier.trigger 自定义结构的协议面,注释成文）、LinkedList 标注维护状态（下游零使用,新代码用 RxList）。资产：deepReview2026H3Round6 的 async-generator loud-fail 组、knownIssuesReproductions 的 packaging 契约 + release tag 推送断言。

  23. 契约条款对抗执行（contract-clause adversarial execution，2026-H3 第七轮）：把 README/接口签名**授予调用方的自由**逐条在最敌意的时序/形态下执行——「返回数组归调用方」就在 batch 窗口内改写到最深一层、「onCleanup 注册清理」就让清理抛错、「destroyComputed 均可」就喂 Rx 类结构、「不产生 unhandled rejection」就在每个 async 收尾路径注错。与方法 5（异常注入）的区别：5 注错在 getter/订阅者等**计算主体**，本轮注错/施压在**契约文本点名的调用方权利**上，暴露的是"承诺面与实现面的窗口差"。动态复现并修复六项：**R7-1** reorder 协议载荷的 Order 对与调用方共享——round5 D1 的 toProtocolPayload 只做外层 slice，内层 [from,to] 对仍是调用方对象，batch 窗口内复用/改写 pair（对象池是原生预期）静默毒化全部 patch 消费者（map 派生 silent 乱序）；修复为 trigger 侧深拷到 pair 一层（与 onChange 出口的 copyNestedArray「外层+一层嵌套」对齐——**同一载荷在两个出口深度不一致即缺陷信号**），内部调用方（swap/sortSelf 自建、patch 再派发已拷贝的 argv[0]）经 `pairsOwned` 参数跳过（swap 热路径白付 O(pairs) 实测 +21%，干净进程 ABBA；加参后全列噪声内）。**R7-2** RxSet.toList × replace：replace 采纳的新 Set 决定全部成员迭代序（含存活成员相对顺序），[newItems,deletedItems] 增量只能改成员不能改序——「增量 ≡ 全量重算」在有序 RxList 上**含序**，错误恢复/force recompute 的重建让同一集合状态呈现不同行序；修复为 toList × replace 回退全量（RxMap.keys × replace 先例，兄弟实现点一致），broadOperators/modelComparison 的 toList 断言从 sortNum 弱化投影强化为含序比较（Oracle 完备性纪律的实例：对 Set 断内容、对 List 断序）。**R7-3** prepareRecompute 的用户钩子窗口（onRecompute/onCleanup 回调、context.onCleanup 清理）位于 setStatus(RECOMPUTING)/inPatch=true 之后、getter 的错误恢复保护之外——钩子抛错一次：同步结构 status 永久卡 RECOMPUTING（此后每次上游写入都对 mutator 抛误导性的"detect recompute in sync recompute"断言、数据永久陈旧），async patch 结构 inPatch 永久卡 true（所有触发只排队 info 永不消化，**静默**冻结；axii FunctionHost 把 context.onCleanup 直接暴露给用户渲染函数）；修复为 prepareRecompute 与 getter 同一处置（handleRecomputeError 复位后 rethrow，全形态横扫：sync full/sync patch/async getter/async patch）；同类：dirty 派发改状态写入先行（持续抛错的 dirty 监听者从"永久冻结在 CLEAN"降为有界陈旧）。**R7-4** async 收尾阶段（finishFullRecompute 的 digest / finishPatchRecompute 的 sendTriggerInfos / patch 回退触发的全量重算）异常发生在 .then 回调里且 settleCleanPromise 被跳过——下游订阅者抛错直接 unhandled rejection（README §5 反例，Node 默认崩进程）+ await recompute() 永久挂起；修复为 settle 进 finally + 两条 async 链尾 console 兜底（错误属下游，本 computed 数据/状态已完成写入）。**R7-5** destroyComputed/recompute/getComputedInternal 对 Rx 类结构（不经 computed() 工厂注册）是 cryptic 的 `undefined.destroy` TypeError，而 README §6 承诺两者均可——修复为注册表未命中时回退 Computed 实例本身。**R7-6** destroy 后惰性 meta（length/keys/values/entries/size）首读重建**活的** computed 订阅已销毁 source（值是正确快照但 effect 常驻、逃过一切销毁路径）——修复为创建后立即随葬（快照值保留，零残留订阅）。验证基线：全套 977 + prod 942 + type-check + build 全绿；axii（53 文件 672 tests）/axle（临时 alias data0→HEAD，33 文件 459 tests）反向确认全绿；audit-reachability 全过；干净进程 ABBA（15 轮取中位）write-atom×computed −0.9% / push×map −0.9% / swapHeavy −1.7% / setToList +2.6%，噪声内（同进程双模块加载的 ABBA 因 IC 污染 A/A 即 ±16%，不可用——干净进程是本级别改动唯一可判读的形态）。资产：`__tests__/deepReview2026H3Round7.spec.ts`（六缺陷类实例回归 + R7-1 的 batch×多派生横扫 + R7-3 全形态横扫 + R7-4 无 unhandled 断言 + R7-6 快照语义钉扎）；README 参数契约（Order 对一层所有权）、async 契约（收尾兜底）、生命周期（钩子抛错不冻结、destroy 后 meta 快照）、支持矩阵（toList 拆行:replace → 重算）已同步。未发现反例的探测面：batch 内 destroy 已排队派生（无僵尸 run/双跑 cleanup）、嵌套创建深度 35 层跨 maxMarkerBits 边界（依赖仍触发）、E2E mutator 同步捕获 async patch 链的下游错误后无 unhandled 残留。(b) **反思机械化轮**（同轮追加；反思结论：六个缺陷没有一个落在既有账本的 UNCOVERED 格子上，全部落在"不存在的列"——账本让盲格可见，但**轴的集合本身没有账本**；且 README 的角色被固定成"裁决依据"，从未被当作"攻击面清单"。六项补法全部落地为常驻资产）：① **契约条款账本**（`__tests__/contractClauseInventory.spec.ts`，§3.3 新规则）——README 逐条款登记{逐字引文（漂移检测：改条款必须同步账本）、kind（grant/behavior/obligation/boundary）、资产}，机械强制：引文必须逐字存在于 README、资产文件必须存在、「语义契约」章每个 ### 小节至少一条登记、**grant 类条款（授予调用方权利/可靠性承诺）禁止 NA**（授予了就必须有行使它的资产——R7 六缺陷全部是 grant 类条款的未行使空间）；UNCOVERED 汇总与两本既有账本共同构成立项来源。② **敌意调用方维度进 fuzzKit**（`clobberCallerOwnedArray`，接入 batchReplayFuzz：splice 返回数组 50% 概率深改写 + 新增"公开 reorder + 调用后深改写 Order 对"操作，均在 batch 延迟消费窗口内行使权利）；变异自证：临时把 reorder 载荷回退为浅拷贝，5/7 seed 变红，还原复绿。③ **生命周期钩子入注入表面**（coreSurfaceInventory 新增 `computed.lifecycleHooks` 行，六维过账）+ **错误注入 recovery-probe 通则**（§3.3 新规则：注入用例必须再触发一次并断言自愈——冻结类缺陷在抛错当步不可见）。④ **不变量升格**（§4 新登记项）：「重算生命周期内一切用户代码窗口必须处于 handleRecomputeError 覆盖内」——`sourceInvariants.spec.ts` 静态执法（computed.ts 每个 `this.prepareRecompute()` 调用点必须被 try 直接包裹，含扫描器自检），行为面由 round7 全形态横扫执法。⑤ **Oracle 立法追溯审计**（§3.3 补充规则）：本轮扫存量排序投影断言——toSet 三处补弱化理由注明（RxSet 内容语义，README 矩阵脚注新增「RxSet 迭代序不属承诺面」），toList 两处已在 (a) 强化为含序；新观察纪律生效时必须机械扫描存量断言，禁止"法不溯及既往"。⑥ **兄弟契约实现点差分**（contractClauseInventory 内建组）：同一契约的多个实现点互为 oracle——载荷隔离深度跨出口一致（patch 消费者 × onChange 出口 × 深改写）、replace 回退语义跨兄弟一致（RxMap.keys ↔ RxSet.toList，witness 计数），R7-1/R7-2 的可静态发现形态（toProtocolPayload 与 copyTriggerInfoPayload 深度不一致、keys×replace 回退与 toList×replace 增量不一致）从此有常驻检测。

  24. 闭世界度量的补集挖掘(2026-H3 第八轮):三大机械防线(差分 fuzz/覆盖账本/mutation 审计)全部度量「已存在的代码 × 已枚举的输入 × 已登记的条款」这个闭世界,对**缺失物**构造性无感——mutation 检测多余的代码,永远检测不出缺失的代码(缺失的布尔化、缺失的 assert、缺失的一层拷贝);fuzz 的输入分布由维度清单决定(清单外 = 零概率);账本强制"格子有资产",不度量资产覆盖格子语义空间的比例。本轮沿五条正交轴系统挖掘补集,动态复现并修复九项:**R8-1** filter × truthy 非布尔谓词——isFirstRun 的 `typeof raw !== 'boolean'` 把每次行级重跑误判为首跑,truthy→truthy 重复插行/truthy→falsy 永不移除/falsy→truthy 插错位置三形态静默分叉(修复:存储点布尔化 `!!filterFn(item)`;根因:TS 签名的 boolean 是运行时无人执法的承诺,typed 测试要 `as unknown as boolean` 才能表达攻击——类型纪律越严,测试越系统性避开无类型调用方的真实行为;"回调返回什么"是 R4-1"回调读什么"轴的正交轴,从未出过事故因此从未进账本)。**R8-2** 观察出口(onChange handler/自定义调度器 infos)的副本对 reorder 只拷到 Order[] 一层,pair 与广播共享——handler 行使 README「可自由处置」即毒化兄弟 patch 消费者(map 派生 silent 乱序),reorderInfo 整对象共享同理;根因三层:①条款切分把「观察出口…可自由处置」这句 grant 漏在 C4.4/C4.5/C4.6 的缝里(小节级下界查不出句子级缺失),②C4.6 的 NA 注记写着"由 C4.4/C4.5 资产钉扎"——虚假的覆盖声明,③R7-1 修复注释断言观察出口"早已对齐"且无测试执法(修复:copyTriggerInfoPayload 按协议形状逐 method 拷贝,pair/reorderInfo 深拷;新条款 C4.11 + handler 方向的兄弟差分)。**R8-3** 同一拷贝的反向缺陷:数组型**用户值**(splice 删除项/插入项、RxMap set 的 oldValue、RxSet delete 的 argv、RxMap delete 的 methodResult)被"一层嵌套"启发式无差别 slice,引用身份破坏,按身份记账的 handler 静默失配(修复同上:用户值保引用,协议容器换新;「一层嵌套」在两个方向上都错的教训——拷贝深度必须按协议形状表逐 method 对齐,与 consumerContractReplay 的形状钉扎同源)。**R8-4** indexBy 的 EKC 插入侧、toMap 的 splice+EKC 双插入侧缺重复 key 守卫——全量侧 assert、patch 侧静默覆盖,派生与全量重算永久分叉;根因:**参考实现抛错的输入域是差分 oracle 的结构性禁区**,fuzz 生成器为让 oracle 可用必然避开 assert 域,"增量侧在 assert 域内静默做错事"构造性测不到;entryPointSemanticsInventory 只归档过判等门,唯一性守卫不在语义组(修复:全入口同 assert;守卫对称性成为常驻纪律)。**R8-5** map 行级重算不执行上一轮 context.onCleanup(新注册静默顶掉旧注册,mapFn 每轮分配的资源逐轮泄漏)——与 computed 同名 API"每轮重算前执行"语义分叉;根因:README §6 的承诺是**照实现写的描述性契约**,条款账本的引文锚定反而把缺口固化;R6-1 发现"并行实现模式"轴后只修实例未横扫全部同名 API(修复:重算前先执行,prepareRecompute 同款"先复位再调用";README §6 成文)。**R8-6** RxSet.forEach 的 track 在迭代**之后**——handler 抛错(含"throw 打断迭代"惯用法 + getter 自 catch 的**成功**运行)时订阅零建立,restoreEffectDeps 恢复的是空集,源变更永不重算(永久静默陈旧);RxList/RxMap 的同名方法 track 在前,同形态可自愈(修复:track 先行;同名 API 语义矩阵扫全:replace/replaceData 的采纳 vs 就地、toArray 的活引用 vs 快照两处分叉钉扎为特征测试并入 README §1)。**R8-7** swap 重叠区间静默丢数据(多重集不保持:swap(0,1,2) 于 [a,b,c] 得 [b,c,b])+ 公开 reorder 对越界/重复/非置换 pair 静默出洞、错位——既有断言只查越界不查语义前置条件,"守卫可达性"纪律只测已有守卫开火,没有"每个语义前置条件必须有守卫"的枚举;未定义语义域被生成器排除,差分 oracle 也进不去(修复:swap 重叠 assert 两种构建都拒绝;公开 reorder dev 构建校验子集置换——from/to 集合相等、无重复、界内整数,pairsOwned 内部调用方零成本;README §4 成文)。**R8-8** batch 内非收敛效应环静默死循环——非 batch 同形态被 "detect recompute in sync recompute" loud 喝止,batch 的 digest 队列无界追加,同一语义两个入口一 loud 一 silent;死循环无法用测试表达(先有 bound 才有可断言性,没人加 bound 因为没测试要求它——鸡生蛋死锁)(修复:dev 构建处理量越过 max(初始队列×16, 100_000) 时 console.error 一次,不中断不改语义,挂死变可诊断)。**R8-9** async getter 形态提示 warn 每次重算都打——它是关于 getter 声明形态的一次性事实,不是逐次误用;mutation 审计曾把 warn 文本/次数归"类 d 接受不杀",频率维度被制度性豁免(修复:每实例一次;warn 信噪比进观察面)。宽容性钉扎(未发现反例):findIndex/find/some/every/once 只以真值判断消费回调返回值,构造性免疫 R8-1(`if`/`!` 消费,不存储不比对)。资产:`__tests__/deepReview2026H3Round8.spec.ts`(九缺陷类实例回归 + 谓词返回值形态族差分 fuzz + 观察出口敌意 handler 全协议形状横扫 + 用户值身份钉扎 + 守卫对称性全入口 + 同名 API 特征矩阵)、`fuzzKit.truthyPredicateForms`(返回值形态维度,boolean/number/string/nullable 四形态)、contractClauseInventory 的 C4.11 + handler 方向兄弟差分;README §1(采纳语义分叉)/§4(真值语义、reorder 置换契约、载荷副本深度)/§6(行级重算 cleanup)同步。

### 3.3 覆盖清单纪律（盲格必须可见）

2026-H2 教训：两个存活多轮的缺陷类都落在"攻击轴已存在、但没和该算子/值域相乘"的盲格上（重复值域 × selection、undefined 值域 × toSorted）。方法轮换有清单，覆盖矩阵却没有清单，盲格因此不可见。规则：

- `__tests__/coverageInventory.spec.ts` 是「公开派生算子 × 对抗维度 → 防线资产」的常驻账本，机械强制：引用的资产文件必须存在、清单键必须在原型上、原型新增公开成员必须被分类（派生算子必须逐维度登记覆盖资产，或显式写 `UNCOVERED`/`NA:<构造性理由>`）。
- `__tests__/coreSurfaceInventory.spec.ts` 是响应式核心（computed/notify/reactiveEffect/atom）的「计算表面 × 对抗维度」账本 + **包导出面普查**：src/index 的每个运行时导出必须被分类（核心表面/集合/外围/工具/诊断），新增导出未分类当场失败；核心表面按 basic/errorInjection/destroyTiming/interleaving/batchSession/scheduler 六维登记。两本账本的 `UNCOVERED` 汇总共同构成 review 立项来源。
- **禁止把无覆盖的格子谎报为有资产**；`UNCOVERED` 是显式登记的债务，其汇总是每轮深度 review 的第一立项来源（先盘点盲格，再发明新方法）。
- 新增对抗值域维度时：生成器加进 `__tests__/fuzzKit.ts`（所有 fuzz 共享，"加一次、全算子可用"），清单加一列，全部算子重新过账。禁止在单个 fuzz 文件里私藏值域。
- 每轮 review 至少消掉一批盲格或给出 NA 论证；账本只允许经论证的增长（新算子落地时）。
- **Oracle 完备性纪律（2026-H3 教训，对偶于 UNCOVERED 规则）**：差分比较默认走 `__tests__/stateOracle.ts` 的**全可观察状态**规范化比对（键集 + 逐键内容 + size），禁止手写"只遍历已存在键比内容"式的部分投影——groupBy 空组键残留曾因该投影（空 ≡ 空恒真）在五个 fuzz 资产下存活多轮，方法 11 的朴素参考模型也被同一投影消费掉。任何弱化（只比部分面）必须在断言处显式注释理由；新增派生结构类型先在 stateOracle 补规范化器再写 fuzz。输入维度有账本（coverageInventory），观察维度同样要有清单：oracle 的强度决定差分测试的上界。
- **观察面清单（2026-H3 round3，把上一条的"观察维度要有清单"落地）**：值状态之外，**触发精确度**是常驻观察列——「一次 digest 每派生至多 1 轮 patch + 至多 1 轮全量回退」「无关源零触发」「判等门全入口零触发」由 `__tests__/triggerPrecisionFuzz.spec.ts`（计数器：`fuzzKit.attachRecomputeCounter`，挂 recompute/fullRecompute 事件，无监听者零开销）执法。"结果正确但多做了工作/排错资源"类缺陷（RxMap.replace 幽灵 SET、RxTime 虚假定时器）对一切值 oracle 天然不可见，只有本列能检出；外围模块的定时器/警告断言（rxTimeDeterministic 的 fake-timer 审计）同属观察面。新增观察维度时与值域维度同规则：helper 进 fuzzKit、断言列进常驻 fuzz，禁止一次性 spot-check。
- **契约外形态操作必须以生成器进入组合空间（sweep 禁止作为维度的唯一载体）**：稀疏（越界 set）曾以一次性 sweep 进入体系——该维度从不与操作序列组合，"先 OOB set、再不等长 splice"两步组合任何随机搜索都生成不出，createIndexKeySelection 校正循环崩溃因此在 sweep 的绿格子下存活（sweep 只做了纯尾插，尾插不进任何校正循环：**格子粒度是"算子 × 维度",缺陷粒度是"代码路径 × 维度 × 后续操作"，一个场景点亮的格子不能证明路径穿透**）。现规则：形态操作生成器进 fuzzKit（`performRandomListOp`/`adversarialSetIndex`），`__tests__/sparseOpsFuzz.spec.ts` 以二级 oracle 让契约外输入也有系统性观察——T1 任何一步不崩溃、T2 强制全量重算后 ≡ 各算子 computation 语义的参考实现（每个算子的洞位语义在该文件成文）、T3 恢复后一步契约内操作增量仍自洽。新增契约外输入类必须加进生成器而不是新开 sweep。
- **行形态是 map/filter 家族的常驻生成维度（2026-H3 round4，R4-1 教训）**："传了 index 参数"≠"读了 index 值"——mapFn **执行期**是否读响应式数据决定行走哪条实现路径（storesValue 不升级 / readsIndex 升级为带 index 依赖的 rowComputed / readsItemAtom 升级为带 item 依赖的 rowComputed），三种形态的缺陷面互不重叠。旧 fuzz 全部只存不读，"结构搬移 × 行级重算"的触发序路径从未被进入，reorder 双重搬移潜伏两年（同一提交里的近似测试——读 index 的 map 挂在三层下游而非被 reorder 的列表上——点亮了心智格子）。现规则：行形态 mapFn/模型进 fuzzKit（`indexReadingMapFn`/`valueStoringMapFn`/`itemAtomReadingMapFn` 族），map 类差分 fuzz 必须至少并行 storesValue 与 readsIndex 两列（broadOperatorsFuzz），readsItemAtom × batch 混排常驻（同文件）；新增行形态加进 fuzzKit 而不是私藏。
- **值域必须叉乘进入通道（2026-H3 round4，R4-2 教训）**：主矩阵格子"算子 × 值域"点亮不等于该值域的**所有进入通道**过账——indexBy × undefined 格子亮着（初始数据/删除侧），argv 插入与 EKC newValue 通道零覆盖，patch 插入侧属性读当场 TypeError。五通道 = initialData / argvInsert / ekcNewValue / ekcOldValue / methodResultDelete；"patch 端按值消费元素"的算子在 `coverageInventory.spec.ts` 的 `VALUE_INGRESS_INVENTORY` 逐通道过账（资产：`__tests__/valueIngressChannels.spec.ts`），恒走全量重算的通道标 NA 并给构造性理由。新增值消费型算子必须入表。
- **大 N 是正确性层级不只是性能层级（2026-H3 round4，R4-3 教训）**：全部正确性测试曾 N ≤ 数千，"十万行 replaceData"只是 bench 里的性能契约——spread 实参上限（≈10^5 量级 RangeError）这类规模缺陷对小 N 套件天然不可见。`__tests__/largeScaleSmoke.spec.ts` 以 10^5 量级对每个算子族跑一发关键操作并与模型差分（进 CI；抓上限/栈深/平方放大，不做计时断言）。规模债务清偿（同轮）：groupBy 与 toSorted 的批量增量曾为 O(插入数 × 源长)/O(组数 × n)，已分别以单遍分桶批量路径与阈值回退清偿（见方法 20 (e) 记录），10^5 批量格子已入本层。
- **下游零反馈面按更高权重立项（2026-H3 round4）**：消费者契约回放（方法 13/16）构造性测不到下游不用的面，而这些面没有生产反馈、仓内覆盖是唯一防线（R4-1 的 map(index) × reorder 即零反馈面）。`surfaceClassification.ts` 的 `DOWNSTREAM_FEEDBACK` 按 grep 事实标注每个主矩阵算子（当前 33/33 为 none——两个下游都只把 Rx 结构当原始容器 + RxListHost 消费协议），coverageInventory 机械强制全员标注；下游用法变化时更新标记并补消费者契约钉扎。review 立项时 none 面的 UNCOVERED/薄弱格子优先。
- **契约条款纪律（2026-H3 round7，方法 23 教训）**：README 不只是裁决依据，同时是**攻击面清单**——每句承诺都是待执行的测试生成器，尤其是描述"调用方可以做什么"而非"库会输出什么"的条款（「归调用方所有」「均可」「不产生」「不冻结」）。**授予了但从未行使 = 未测试的承诺**：round7 六个缺陷全部落在 grant 类条款与实现之间的窗口差上，而既有账本对此零命中（它们枚举的是库的行为域，不是调用方的权利域）。规则：`__tests__/contractClauseInventory.spec.ts` 逐条款登记（逐字引文做漂移检测、grant 类禁止 NA、语义契约章每小节至少一条、UNCOVERED 汇总入立项来源）；新增/修改 README 契约条款必须同步账本（引文失配当场红）；新增"调用方权利"类条款必须同步扩展 fuzzKit 的敌意调用方维度（`clobberCallerOwnedArray` 先例——权利在 batch 延迟消费窗口内行使）。
- **错误注入的表面清单必须含全部用户代码窗口 + recovery probe 通则（2026-H3 round7，R7-3 教训）**：注入表面此前按"计算主体"枚举（getter/patch/订阅者/调度器），生命周期钩子（onRecompute/onCleanup/context.onCleanup 清理、dirty/clean 监听者）因被归类为"观察设施"而从未入行——但它们同样是库在重算周期内同步调用的用户代码，且钩子窗口位于状态写入之后。规则：凡"库调用用户代码"的位置都是注入表面（coreSurfaceInventory 的 `computed.lifecycleHooks` 行先例）；**注入用例必须附 recovery probe**——注错之后再触发一次并断言自愈（冻结/挂起类缺陷在抛错当步不可见，只有下一步才暴露；方法 12 的 patch 错误恢复先例升格为通则）。
- **Oracle 立法必须追溯（2026-H3 round7，R7-2 教训）**：观察纪律（如全可观察状态比对）生效时必须机械扫描**存量断言**并逐一补注理由或强化——toList 的排序投影写于 Oracle 完备性纪律立法之前，被"祖父条款"式豁免而多存活数轮。观察维度按**结果类型**选择而不是来源类型（Set 来源的心智"无序"不豁免 List 结果的序观察；RxSet 本身按内容承诺已成文于 README 矩阵脚注）。
- **兄弟契约实现点互为 oracle（2026-H3 round7，模式升格）**：同一契约在多个实现点落地时（同一载荷的多个出口、同一语义的多个派生），实现点之间的保护深度/回退语义不一致本身就是缺陷信号——R7-1（toProtocolPayload 外层 vs copyTriggerInfoPayload 外层+一层）与 R7-2（keys×replace 回退 vs toList×replace 增量）都有"正确的兄弟"在场却无人做差分。规则：已知兄弟对钉成行为差分测试（contractClauseInventory 的「兄弟契约实现点差分」组）；新增"同一契约的第 N 个实现点"时必须与既有实现点差分过账。
- **类型签名域即攻击面（2026-H3 round8，R8-1 教训）**：公开回调的 TS 参数/返回值类型是**运行时无人执法的承诺**——JS 调用方随便违反,而 typed 测试要 `as unknown as X` 才能表达违约输入,类型纪律越严、测试越系统性避开无类型调用方的真实行为(类型系统是覆盖率的天花板而不是防线)。凡「存储回调返回值并以其**类型**驱动控制流」的实现点(filter 的 isFirstRun 先例)必须在存储点归一化,或给 fuzzKit 加违约形态列(`truthyPredicateForms` 先例);新增接受回调的公开 API 时对照本条过账。平台对应物(Array#filter 的真值惯例、原生 splice 的所有权预期)是用户心智模型的真实来源,签名与平台惯例分叉处优先按平台语义收编成契约。
- **grant 条款登记必须枚举行使空间（2026-H3 round8，R8-2 教训）**：grant 的语义空间是「行使者 × 对象 × 窗口 × 方向」的叉乘,一个资产只覆盖一个点——「载荷副本可自由处置」的资产曾只行使"调用方改**原始**数组",handler 改**自己副本**的方向零覆盖,pair 共享因此在 round7 的绿账本下存活。规则:登记 grant 条款时在注释里写明行使空间的各维度与对应资产(C4.11 先例);「每小节至少一条登记」是小节级下界,**新增/改写 README 契约句时必须逐句对照账本补条款行**——条款切分的缝隙(整段引用被三条条款瓜分,漏掉中间一句)不触发任何红灯,只能靠切分时的逐句纪律。
- **注释里的事实声明是未验证假设（2026-H3 round8，R8-2 教训）**：修复注释中的「早已对齐/无需处理/构造性安全/由 X 钉扎」每一句都是零执法的假设——R7-1 的注释声称观察出口"早已按外层+一层拷贝",对 reorder 为假,后续 review 把注释当证据引用。规则:写下此类声明时必须同步指向证明它的行为测试(没有就先写测试);review 时把"注释声称 X"一律当作"待验证假设 X"处理,不得作为证据等级里的"静态确认"。
- **守卫对称性审计（2026-H3 round8，R8-4 教训）**：**参考实现抛错的输入域是差分 oracle 的结构性禁区**——fuzz 生成器为让 force-full-recompute oracle 可用必然避开 assert 域(重复 key、未定义语义的重叠区间),于是"增量侧在该域内静默做错事"对整个差分体系构造性不可见。规则:凡全量 computation 侧 assert 的关系不变量(唯一性、有界性),patch 的**每个插入/替换入口**必须同 assert(indexBy/toMap 先例;与 §3.1 第 3 问的"同一语义全部入口"同构,但轴是守卫而不是判等);凡语义前置条件(swap 区间不重叠、reorder pairs 构成置换)必须有守卫或显式契约外声明,"守卫可达性"纪律只测已有守卫开火,不能代替前置条件的枚举。新增带 assert 的全量 computation 时对照本条过账。
- **不可测语义先给 bound/观察列（2026-H3 round8，R8-8/R8-9 教训）**：死循环(非收敛效应环)、警告频率(信噪比)、副本身份这类语义没有 bound/观察列就没有可写的测试——"没人加 bound 因为没测试要求它"是鸡生蛋死锁,打破方向恒为先给机制再钉测试。已落地:digest 处理量的 dev 诊断阈值(R8-8)、warn 的每实例去重(R8-9)、载荷副本的身份断言(R8-3)。同类候选(计时器数量、微任务队列深度)出现时同规则处置;mutation 审计把 warn 文本/次数归"接受不杀"不豁免**频率语义**本身的观察义务。

### 4. data0 特有的 review 检查（附资产追溯）

每条检查项必须指向实现它的常驻资产；新增检查项而不补资产，视为该项未完成。

- 立案前先对照下方「架构决策与已知语义边界」一节：其中列出的行为是既定架构语义，观察到相关现象时引用该节说明，不得作为缺陷报告或擅自"修复"。
  资产：`__tests__/architectureSemantics.spec.ts`、README「架构语义」节。
- `RxList` 派生算子必须验证：每步增量结果等于从当前 `source.data` 全量重算的结果，**含 batch/延迟调度下多条 triggerInfo 单次 digest 重放的序列**（triggerInfo 的 key/argv 是操作时位置，source.data 是重放时终态，patch 端凡按终态解释操作时位置都是缺陷）。多 info 重放**必须经 `src/digestReplay.ts` 内核**取每条 info 操作时的源状态快照（构造性关死该缺陷类；2026-H3 起 groupBy/slice/findIndex 由此从"多 info 回退全量"升级为增量），禁止在 patch 里手写终态回推；内核判不可重建或语义仍无法增量时用 `return false` 回退全量重算，并同步 README 支持矩阵的脚注。
  资产：`__tests__/digestReplay.spec.ts`（内核逆操作 + batch 内逐操作实录的差分地面真值）、`__tests__/broadOperatorsFuzz.spec.ts`（全算子差分）、`__tests__/duplicateValuesFuzz.spec.ts`（重复值域差分）、`__tests__/batchReplayFuzz.spec.ts`（batch 多操作重放差分 + toSorted 等值 tie 差分）、`__tests__/lifecycleAndReplayFixes.spec.ts`（最小复现回归）。README 的支持矩阵中每个"增量"格子必须有差分覆盖。
- mutation 测试至少覆盖 splice 的负数、越界、小数、`NaN`、`-0`，重复值，`set`，`reorder`，batch，以及回调抛错。
  资产：`broadOperatorsFuzz` 的操作生成器（对抗参数域）、`batchReplayFuzz` 的 batch 操作生成器、`__tests__/reproducedIssuesFixes.spec.ts`、`__tests__/reviewFixes.spec.ts`。
- 响应式回调变更后，除结果外还要验证依赖仍会触发、被删除 effect 已销毁、全局 tracking/session 栈恢复。
  资产：dev 不变量断言（`batch`/effect run 的栈深复原、digest 静止态、`RxList` 行级记账对齐——违约当场抛错，被全套测试被动执行）、`__tests__/invariantAssertions.spec.ts`（断言开火自检）。
- destroy 语义必须对称：destroy 后不再接收更新（僵尸检查）、create+destroy 后活跃 effect 计数回到基线（泄漏检查）、源模式结构与计算模式一视同仁（destroy 事件、children、惰性 meta、`context.onCleanup`）。子类的销毁清理必须放进 `destroyResources` 钩子（唯一会被所有销毁入口——实例 `destroy()`、`destroyChildren`、`destroyComputed`——执行的位置），不得放在 `destroy()` 覆写里。已销毁结构的变更方法是 no-op。
  资产：`__tests__/destroySemantics.spec.ts`（全派生族僵尸/泄漏横扫）、`__tests__/lifecycleAndReplayFixes.spec.ts`（destroy 取消在途 async patch、no-op 变更）。
- async patch/getter 的并发行为必须经交错枚举验证，不允许只测"启动→等待→断言终值"的单一顺序；destroy 与在途 async patch 的交错也在此列。
  资产：`__tests__/asyncPatchInterleavings.spec.ts`（两个 async patch + 中途写入的全排列）、`__tests__/lifecycleAndReplayFixes.spec.ts`（destroy × 挂起 patch）。
- 涉及 axii/axle 的 `triggerInfo.argv` 原始参数契约时，不得直接修改外部协议；内部消费者应独立归一化。
  资产：`deepReviewFixes.spec.ts`（argv 透传契约测试）、README「RxList 参数契约」节。
- 消费 `triggerInfo.oldValue`/`newValue` 的派生结构不得用 `!== undefined` 判断"有无"——undefined 是 RxList 的合法元素值，协议里"值为 undefined"与"无值"带内不可区分。无法按 key/methodResult 定位时回退全量重算（toSorted 先例）。加性 `hasOldValue` 字段的提案经 2026-H2 评估暂不引入：回退已保证正确性、无消费者需要 undefined 的增量处理，单方面加协议面属投机设计；若未来出现真实需求，须与 axii/axle 同步做加性扩展。
  资产：`deepReview2026H2Findings.spec.ts`（undefined 值域 sweep）、本条规则。
- 内部记账 key（depsMap、行级 Map 等）不得与用户数据值域共享命名空间：协议订阅一律用内部 Symbol（`METHOD_TRACK_KEY`/`EXPLICIT_KEY_CHANGE_TRACK_KEY`/`ITERATE_KEY` 先例），新增"以固定值为 key 的订阅面"必须用 Symbol 而不是字符串常量；带内合法值（undefined、NaN、协议同名字符串）必须能作为普通数据 key 正常工作。
  资产：`deepReview2026H3Round2.spec.ts`（协议字符串 × RxMap/RxSet/groupBy/indexBy/toMap/toSet/selection 横扫 + undefined key 回归）。
- 长活 source 上的订阅记账必须有界：谁创建记账条目，谁负责在退订到空时回收（depsMap 的 host 摘除、`pruneIndexKeyDeps` 的重同步）；新增"按 key 建 dep/缓存"的结构时必须给出回收路径，并用 `scripts/audit-reachability.mjs` 式的 churn 探针验证有界性。destroy 后完成的 async 收尾不得重放订阅（active 门）。
  资产：`deepReview2026H3Round2.spec.ts`（churn 有界 + 错误恢复挂回 + 僵尸重订阅回归）、`scripts/audit-reachability.mjs`（真实 GC 审计）。
- **不变量升格（2026-H3 round4）**：CAUTION 注释中含"必须/一定/否则"的**跨方法**约束，不得只以注释形态存在——必须升格为下列三种之一：(1) 构造性原语（唯一合法出口，如 `removeFromActiveScopes`/`addDep`/`dispatchStructuralThen`），(2) dev 不变量断言，(3) `__tests__/sourceInvariants.spec.ts` 的静态执法（可静态判定的模式 + 允许清单）。教训：R4 三缺陷的共同形态是"兄弟实现点不一致"——不变量在某处被正确编码（doSplice 的 session 顺序、spliceMany 的存在动机、computation 的 null-skip），别处绕过而无机制报警。当前登记项：结构 info 先于订阅者可见的原子写（`RxList.dispatchStructuralThen` 原语，doSplice/reorder 唯一出口）；不定长数组禁止 spread 进函数调用（sourceInvariants 的允许清单审计）；patch 插入侧值域守卫与全量对称（valueIngressChannels 行为执法）；**重算生命周期内一切用户代码窗口必须处于 handleRecomputeError 覆盖内**（2026-H3 round7 R7-3：`prepareRecompute()` 调用点必须被 try 直接包裹——sourceInvariants 静态执法 + round7 全形态横扫行为执法；同类的状态写序规则：状态位写入先于用户钩子派发，dirty 先例）。新增变更方法/新写 CAUTION 跨方法约束时对照本清单登记。
  资产：`src/RxList.ts` 的 `dispatchStructuralThen`、`__tests__/sourceInvariants.spec.ts`（含扫描器自检）、`__tests__/valueIngressChannels.spec.ts`、`__tests__/deepReview2026H3Round7.spec.ts`（R7-3 组）。

### 5. Review 输出格式

每份深度 review 至少包含：

- **已动态复现**：最小复现、实际/预期、影响范围。
- **已静态确认**：静态证据及为什么不适合或尚未进行运行复现。
- **待验证**：还缺少什么证据。
- **验证基线**：测试、类型检查、构建及其真实结果。

不得使用“所有问题均已复现”之类的总括表述，除非报告中的每一项和每个影响声明确实都有对应的执行证据。

## 架构决策与已知语义边界（不作为缺陷处理）

以下三项是与 data0 架构绑定的**既定语义**。它们都曾在深度 review 中被动态复现并完整评估过修复方案，结论是：修复与当前架构冲突过大（等价于重写传播引擎或推翻性能契约），维护者明确决定**不修**。

约定：

- Review 中再次观察到这些现象时，归类为"架构语义"，引用本节即可；不得作为缺陷立案，不得提交"修复"。
- `__tests__/architectureSemantics.spec.ts` 用特征测试（characterization test）固定这些行为。测试失败时先判断：无意的行为漂移 → 修代码恢复测试；有意的架构变更 → 必须与维护者确认方向，并同步更新本节与该测试。
- 每项都给出了"仍属缺陷"的边界：边界内的问题照常按证据纪律立案。

### A1. 急切推模式允许菱形依赖 glitch

- **语义**：atom 写入后按订阅顺序同步推播（`Notifier.trigger`/`triggerEffects`），无拓扑排序、无读时拉取。菱形依赖（`a→c` 且 `a→b→c`）下，先订阅的下游会以"新 a + 旧 b"先算一遍：可观察到系统从未处于的中间值，并产生重复重算。终值必然收敛正确。
- **不修的原因**：computed 急切求值与 `applyPatch` 的"触发即逐条重放 triggerInfos"都建立在推模式上。glitch-free 需要拉模式惰性求值（Vue 3）、拓扑排序调度（MobX）或版本校验（preact signals），任一方案都等于重写传播引擎，并推翻 batch/immediate/增量 patch 的现有语义。
- **仍属缺陷**：终值不收敛或终值错误；同一 effect 在单次无环传播中被无限触发；`batch()` 结束后结果与全量重算不一致。

### A2. `batch()` 内读 computed 返回进入 batch 前的值

- **语义**：effect session 把订阅者的执行推迟到 digest，"标脏"（`handleTriggered` 中置 `STATUS_DIRTY`）也随 run 一起被推迟；而 computed 的读路径没有"脏则重算"的拉取。因此 batch 内"先写依赖、再读该依赖的 computed"读到的是旧值。atom 本身的读取不受影响（写入立即生效，推迟的只是订阅者）。
- **不修的原因**："推迟副作用"是 batch 的存在意义；读时拉取与推模式核心冲突（同 A1）。`autorun`/`once` 已通过 `preventEffectSession` 独立保证自身场景的读写一致。
- **仍属缺陷**：batch **结束后**读取仍是旧值；batch 内读 atom 本身拿到旧值；digest 丢弃排队的 effect 导致永久陈旧。

### A3. 构造与 `replace` 采纳外部容器引用（所有权移交）

- **语义**：`new RxList(arr)`、`new RxSet(set)`、`new RxMap(map)`、`RxSet.replace(set)` 直接持有传入容器（零拷贝，`list.data === arr`）。调用方视为移交所有权：之后必须通过 Rx 实例的方法修改；绕过方法直改原容器不会触发任何通知，派生结构与源静默失联属于**契约内行为**。
- **不修的原因**：data0 没有 Proxy 包裹，响应性完全建立在"方法即变更边界"上；无条件防御性拷贝违背大数据量场景（十万行 `replaceData`）的性能目标。
- **仍属缺陷**：通过 Rx 方法修改却漏触发或触发错误；新增采纳外部引用的入口而未在注释中声明所有权移交。

## 常用验证命令

```bash
pnpm install --frozen-lockfile
pnpm test --run
pnpm test:prod        # 生产语义差分(__DEV__:false,排除 dev 特化文件)
pnpm type-check
pnpm build
pnpm exec vitest run --coverage
```

锁文件安装失败本身应作为仓库问题报告，不能静默改用无锁安装后声称 CI 基线正常。

以上基线由 CI（`.github/workflows/ci.yml`）在每个 push/PR 上强制执行。热路径改动必须附 `pnpm bench` 对比（分支 vs main）。

## Mutation 审计（周期性，不进 CI）

行覆盖率不度量测试的检出能力（95% 覆盖曾对多个致命缺陷零预警）。用 mutation testing 度量"测试套件到底能杀死多少注入错误"：

```bash
# 限定单模块审计（全量运行过慢，按模块轮换；默认配置见 stryker.config.json）
pnpm mutation                                    # 默认 mutate src/util.ts
pnpm exec stryker run --mutate 'src/dep.ts'      # 指定其他模块
```

- 建议在每轮深度 review 前对本轮重点模块跑一次，幸存的 mutant 即测试盲区，优先补差分/性质测试而不是逐 mutant 补例子。
- **排期按风险而不是按轮换**：体积 × 分支密度 × 距上次审计时间。RxList.ts（最大、分支最密的模块）2026-H2 首跑即暴露 479 个幸存 mutant——绝对量远超先审的小模块，验证了风险优先的必要性。
- **闭合判据（2026-H3 round3 起）**：`src/` 每个模块必须**至少完成一次首轮审计**；新增源文件必须在下一轮深度 review 前首审。教训：风险感知锚定在"以前在哪找到过 bug"，被所有测试间接经过的中枢模块（notify.ts）产生"肯定被测得很透"的覆盖幻觉，18 轮从未首审——首跑即暴露一整条不可达死分支与 3 个检出盲区，且 covered-mutant 得分仅 74.87%（间接经过 ≠ 检出力）。首审状态账本见下方基线记录（2026-07 起全模块闭合）。

### 性能优化的行为特征差分（2026-H3 round3 起）

性能类改动必须在合并前对照**行为特征矩阵**：跑全套测试之外，重点核对 `__tests__/creationShapeMatrix.spec.ts`（atom 形态选择 × 能力矩阵）与各特征钉扎资产；行为窄化必须显式记录进 README 契约并同步特征测试，禁止静默通过。教训：primitive atom 快路径（5f15d2a，2026-05）commit message 声称 "preserving object atom behavior"——对「生而对象」的 atom 为真，对「生而 null 后来写入对象」的 atom 为假（proxy set 陷阱按当前值分派，primitive 形态没有陷阱，属性写静默落在函数对象上），而 axii 最高频的 `atom(null)` 惯用法贴着这条边界走。该窄化没有让任何测试变红，因为"形态选择规则"此前从未被枚举——**测试镜像文档，未被枚举的语义无从失败**；特征矩阵的职责就是把这类"没人写下的规则"变成会失败的表。
- 幸存 mutant 涉及「架构决策」语义的，对照该节判断是否属于刻意未断言的行为；命中长期无人使用的辅助代码（如 Vue 继承的字符串工具）时，优先考虑删除死代码而不是补测试。
- 基线记录（用于观察趋势）：
  - 2026-07 首跑 `src/util.ts` —— 行覆盖 98.66%，mutation score **65.94%**（240 killed / 113 survived / 12 no-coverage，48s）。行覆盖与检出能力的差距即测试盲区的量化。
  - 2026-07 修复轮跑 `src/reactiveEffect.ts`（destroy 核心重构后）—— 行覆盖 96.1%，mutation score **73.10%**（209 killed / 3 timeout / 71 survived / 7 no-coverage，94s）。
  - 2026-07 评估修复轮：删除 Vue 遗留且无生产引用的字符串/指令辅助（`isOn`/`camelize`/`isReservedProp` 等），降低 util 中"无人使用却撑高幸存 mutant"的死代码面。
  - 2026-07 方法 10 轮首跑 `src/RxList.ts` —— mutation score **72.08%**（1401 killed / 37 timeout / 479 survived / 78 no-coverage，4m52s；测试套为 selection/undefined 修复后、duplicates/undefined 扩展 sweep 落地前）。479 个幸存 mutant 为当前最大单模块盲区账本，下轮 review 与 coverageInventory 的 UNCOVERED 格子共同作为立项来源。
  - 2026-07 核心账本轮首跑 `src/computed.ts` —— mutation score **68.04%**（369 killed / 10 timeout / 149 survived / 29 no-coverage，3m17s；coreLifecycleGaps 资产落地前）。核心模块的检出率低于集合模块，与"核心平面此前无账本"的判断相互印证。
  - 2026-07 燃尽轮重测（coreLedgerBurndown/collectionLedgerBurndown/consumerContractReplay/weirdNumbersFuzz 落地后）——`computed.ts` **70.13%**（381 killed / 11 timeout / 146 survived / 21 no-coverage，+2.09pt，no-cov 29→21）；`RxList.ts` **72.19%**（1440 killed / 37 timeout / 490 survived / 79 no-coverage，+0.11pt；总 mutant 数随新代码路径增加，killed +39）。账本燃尽对核心模块的检出提升显著、对 RxList 的边际提升有限——RxList 的 490 幸存 mutant 需要按等价类专项消化（差分 fuzz 的断言粒度问题），而不是继续加维度，此为下轮立项方向。
  - 2026-07 幸存 mutant 等价类分析（490 个全量分类）：**最大等价类（约 70%，ConditionalExpression 222 + EqualityOperator 91 + LogicalOperator 28，集中在 slice/map/doSplice/toSorted/concat 的增量路径条件与区间算术）= "把增量 patch 变异成回退全量/等价慢路径"——结果仍 ≡ 全量重算，差分 fuzz 构造性杀不死（差分只证结果不证增量性）**。对策：`Computed` 增加 `fullRecompute` 见证事件（无监听者零开销），新资产 `__tests__/incrementalityWitness.spec.ts` 对 README 矩阵逐格断言「增量格子契约内单 info 操作零回退 + 重算格子回退确实发生」，双向钉死增量性语义。次大类：StringLiteral 30（断言/警告消息文本，行为等价，接受不杀）；其余 BlockStatement/OptionalChaining 多为防御性清理路径，由 destroy/稀疏 sweep 渐进覆盖。
  - 2026-07 见证资产落地后重测 `RxList.ts` —— mutation score **74.93%**（1507 killed / 41 timeout / 439 survived / 79 no-coverage，+2.74pt，killed +67，survived 490→439）。增量性见证单资产杀掉 51 个幸存 mutant，验证等价类判断；剩余 439 中约 30 为行为等价（消息文本/性能快慢路径选择），其余作为渐进债务随各 sweep 演进。
  - 2026-07 `src/digestReplay.ts` 内核落地跑 —— 首跑 70.00%（91 killed / 38 survived / 1 no-cov）；补"协议外合成 info 直喂内核"的守卫可达性测试后 **83.85%**（109 killed / 20 survived / 1 no-cov，30s）。剩余幸存为防御等价类：`methodResult ?? []` 与可选链（协议内恒存在）、reorder 越界检查的单子句变异（其余子句仍拦截）、`new Array(n)` 容量提示（按索引赋值语义等价）——均归入「安全方向/防御分支」接受项。
  - 2026-07 方法 18 轮首跑 `src/dep.ts`（host 记账落地后）—— mutation score **61.49%**（90 killed / 1 timeout / 38 survived / 19 no-coverage，67s）。dep.ts 此前从未单独审计；幸存主体分类：CompactDep 的溢出/降级路径子句（升级 Set/收缩回 single 的边界，由 primitive atom 行为间接覆盖但无专项 killer）、Vue 继承的 dep-marker 机制（wasTracked/init/finalize 的位运算子句——行为由全套 computed 测试被动钉住，单子句变异多为等价慢路径）、新增 host 记账的防御分支（`reattachDepToHost` 的 merge 分支与 `pruneEmptyDepFromHost` 的身份检查：当前调用序下构造性接近不可达——cleanup 的 prune 门控在 `!active`、marker 模式无 pre-cleanup——保留是防未来调用序变化，归入方法 15 裁定的「防御分支」接受项）。
  - 2026-07 方法 19 轮首跑 `src/notify.ts`（此前从未单独审计）—— mutation score **68.90%**（288 killed / 100 survived / 30 no-coverage，3m42s）。幸存分类：no-cov 主体 = triggerEffects 的 CompactDep 快路径（**不可达死分支**——CompactDep 只挂在 primitive atom 函数对象上，派发一律走 triggerPrimitiveAtomValue 特化路径，从不进 targetMap；本轮删除，ABBA 微基准 −0.99% 噪声内）+ track/trigger 监听者派发 payload（诊断面）；survived 主体 = dev 断言子句/消息文本（类 d）、快慢路径选择（空 session 快出口、hasMultiple 去重升级、normalizeTrackKey 先比 type 的短路，类 a）、防御分支（digest finally 复位循环、getDepEffects 空守卫，类 d）、被内部调用形状遮蔽的守卫（协议 key 归一化对内恒等、activeEffect 自触发抑制的逐派发形态变体，类 c）。真实检出盲区 3 个已补杀：triggerPrimitiveAtomValue 的 shouldTrigger 暂停门（既有覆盖只走 keyed trigger）、scheduleAtomEffect 的 session info 形状与逐次累积（既有 batch × atom 覆盖都用不消费 info 的 effect）、多 dep 命中的恰一次去重派发。死代码删除 + 补杀后重测 **72.61%**（289 killed / 97 survived / 12 no-coverage）。
  - 2026-07 方法 19 轮首跑 `src/RxSet.ts`（此前从未单独审计）—— mutation score **90.39%**（254 killed / 27 survived / 0 no-coverage，44s）。集合模块最高分；幸存全部有覆盖，主体为 destroyed no-op 警告文本、per-key info 字段（RxSet 无 key 级订阅面，字段无消费者而被遮蔽）与"delete 非成员误触发"类幽灵 info（下游 patch 幂等吸收），归入类 c/d 接受项，无检出盲区立案。
  - 2026-07 round3 改动后重审 `src/RxList.ts`（本轮改动最大模块：isDenseIndexKey 守卫 ×8、filter 全量重建监听、groupBy 全下标扫描、concat 段长守卫、indexKeySelection 洞位 ?.、selection resetIndicators）—— **80.64%**（1731 killed / 43 timeout / 391 survived / 35 no-coverage；对比上轮 81.47%/1617/345：总 mutant 随新守卫代码增长，killed +114）。新代码幸存逐一分类均落入接受项：isDenseIndexKey 单子句与守卫条件变异 = "忽略 ≡ 回退全量"的安全方向等价（非稠密 EKC 的 oldValue 恒 undefined，旧回退路径兜底）；filter 重建监听的 `rebuildAfterPatch=false`/`if(length)` 变异 = 强制重建/空 splice 的结果等价；concat 段长守卫循环变异 = 恒回退全量的保守方向；载重变异（listener 的 `pending=null`、full-scan 主体）均被 R3-6/R3-7 回归杀死。聚集幸存(1509/1636/1674×5)为 dev 断言子句/消息文本（类 d）。无新立案。`atom.ts` 64.36% → 补 no-cov 盲区（proxy 形态的 call 转发/Symbol.toPrimitive/具名 atom——此前只对 primitive 形态测过）后 **77.66%**（146/42/0），剩余幸存为 toPrimitive hint 分支等价与 isPlainObject 双调用等价类；`RxMap.ts` **84.12%**（143/21/6）；`common.ts` **74.07%**（40/14/0，幸存主体为 nextJob 兜底 console.error 文本与 once 双保险分支）；`RxTime.ts` **79.26%**（107/25/3）；`AsyncRxSlice.ts` **89.39%**（59/7/0）;`LinkedList.ts` **77.03%**（54/16/1）；`manualCleanup.ts` **85.71%**（12/2/0）；`debug.ts` **100%**（19/0/0）；`retainedDiagnostics.ts` **51.33%**（77/71/2，诊断计数器——只被诊断测试消费，幸存主体为快照字段算术，检出价值低，接受不追）；`operations.ts`/`flags.ts` 零 mutant（纯常量）。
  - 2026-07 round4 改动后重审 `src/RxList.ts`（reorder session 化、indexBy/toMap 插入侧守卫、slice spliceArray）—— **80.86%**（1751 killed / 45 timeout / 390 survived / 35 no-coverage；对比 round3 后基线 80.64%/1731/391：killed +20，survived −1）。新改动行无幸存 mutant；顺带补杀既有盲区 1 个：indexBy/toMap 的非稠密 key EKC 守卫（R3-5 幽灵 EKC 等价类的 indexBy/toMap 实例此前无直接钉扎，`set(-1,x)` 幽灵 entry 回归已入 round4 资产）。其余幸存复核为既有接受项（dev 断言子句/防御分支/安全方向变异）。
  - 2026-07 round4 补法落地后重审 `src/RxList.ts`（dispatchStructuralThen 重构 + 行形态 fuzz + 守卫 killer + 大 N 层入套件）—— **81.11%**（1754 killed / 49 timeout / 385 survived / 35 no-coverage；对比补法前 80.86%/1751/390）。第 (e) 类首个立案项闭合：hasPendingStructuralInfos 守卫条件的幸存 mutant 被 batch 先后两序 killer 杀灭（守卫行不再出现在幸存清单）。审计时长 ~9.5 分钟（大 N 冒烟层入套件后与此前同量级，可接受；如未来显著拖慢可在 stryker 侧忽略该文件并在此记录）。
  - 2026-07 round4 规模债务清偿后重审 `src/RxList.ts`（groupBy 单遍批量 + toSorted 阈值门，总 mutant 2223→2299）—— **80.99%**（1809 killed / 53 timeout / 402 survived / 35 no-coverage；killed +55）。新代码域幸存逐一分类均落入接受项：阈值双门比较符 ±1（回退恒安全，边界带内快慢等价，类 a——boundary 测试钉住 65 增量/300 回退两侧）、单项快路径选择条件（两路径语义等价由批量差分 fuzz 的快慢交替证明，类 a）、删除 clamp 与 `?? []`（协议内不可达的防御，类 c）、空桶 if 门（空循环等价，类 a）、fast-path 内 `startIndex + index` 的 index 恒 0（被 ≤1 守卫构造性遮蔽，类 c）、dev 断言子句（类 d）。无 (e) 类立案项。
  - 2026-07 方法 15 债务消化轮。立项前重跑基线（环境漂移校准）：`RxList.ts` 74.70%（1523 killed / 39 timeout / 449 survived / 80 no-coverage）、`computed.ts` 70.18%（382 killed / 11 timeout / 146 survived / 21 no-coverage）。资产 + 死代码清偿落地后——`RxList.ts` **81.47%**（1617 killed / 41 timeout / 345 survived / 32 no-coverage，+6.77pt；no-cov 80→32 主要来自 findIndex 死缓存删除）；`computed.ts` **76.37%**（406 killed / 11 timeout / 119 survived / 10 no-coverage，+6.19pt，no-cov 21→10）。**等价类裁定修正**：此前"剩余约 30 为行为等价"严重低估——本轮逐 mutant 分类确认剩余幸存以「安全方向变异」为主体：(a) 把增量条件变异成"强制回退全量/强制慢路径"（结果仍 ≡ 全量重算，如 doSplice 的 isPureAppend/isPureClear 快路径与慢路径可观察等价、slice 的负 end 单 info 回退是保守而非必要）；(b) 把校正区间变异成"放宽为全量重写"（幂等写 + atom 判等去重使超集校正不可观察，如 createIndexKeySelection 的 affectedRange）；(c) 被构造性防线遮蔽的内部错误（binarySearchFind 的二分变异被 Object.is 线性兜底修正、atomIndexes 初值变异被后续校正循环覆写）；(d) dev 断言消息文本与断言子项（可达状态下其余子项仍然成立）。这些类**定义上不可由行为测试杀死**，属于接受项而非债务；真实债务（守卫可达性、协议字段值、触发精确性、状态机转换序）已由本轮资产钉住。后续轮次不应再以"分数逼近 100%"为目标，而以"新幸存 mutant 必须能归入接受类之一，否则立案"为验收标准。
  - 2026-07 round5 裁定执行后重审 `src/RxList.ts`（载荷防御拷贝、normalizeIndexKey、dev 纯度探测落地后）—— **80.05%**（1886 killed / 48 timeout / 445 survived / 37 no-coverage；总 mutant 随新守卫/探测代码增长到 2416）。新代码域幸存逐一分类：normalizeIndexKey 外层 typeof 门的 `if(true)` 变异 = 等价慢路径（number key 在 `String(n)===key` 上恒 false，类 a）；**真实检出盲区 2 例已补杀**（守卫子句删除——只留 `String(n)===key` 时 "2.5" 被误归一化,由「非规范字符串协议 key 身份钉扎」杀死；`sameValueZero` NaN 分支删除——NaN 组键被误报不稳定,由「NaN 组键零误报」杀死；均经变异自证:施加 mutant 确认 killer 变红后还原）；dev 探测门/warn 文本/`checked=false` 重复探测变异 = 类 d（warn 语义,断言不计次）；probe 的 `find(it=>true)` 变异被后续 null 守卫遮蔽（类 c）；indexBy 全量循环 `<=` 边界变异被 null-skip 吸收（类 a）。其余幸存与 round4 基线同域,无新立案。
  - 2026-07 round7 改动后重审 `src/computed.ts`（prepareRecompute 错误恢复、async 收尾 catch、settle-in-finally、resolveComputedInternal 落地后）—— **77.32%**（439 killed / 11 timeout / 124 survived / 8 no-coverage；对比方法 15 后基线 76.37%/406/119：总 mutant 随新错误路径增长到 582，killed +33，+0.95pt）。新代码域幸存逐一分类均落入接受项：prepareRecompute catch 的 `recomputeId === recomputeId` 守卫单侧变异 = "钩子内重入 destroy" 的防御分支（变异后 handleRecomputeError 在已销毁实例上运行，settle 函数已删除、recomputeInternal 有 active 门，无可观察损坏——套件不构造"钩子既 destroy 又抛错"，与既有 getter catch 的同名守卫同类接受）；`willPropagate` BooleanLiteral 变异 = console 双报告（安全方向，类 d）；两条 async 链尾 catch 的消息文本（类 d）。fail-before/pass-after 已由 stash 差分证明（修复前 20/23 资产用例变红）。
  - 2026-07 round7 发版前复审其余三个改动模块。`src/RxSet.ts`（toList×replace 回退落地后）—— **90.56%**（259 killed / 27 survived / 0 no-cov）：新代码域幸存 = toList patch 的 `deletedItems` 初值 ArrayDeclaration（add 分支下改初值只多一次 findIndex miss，类 a）与 `index !== -1` 守卫（delete 事件的成员恒在列表，协议内不可达防御，类 c），其余与方法 19 首审同域。`src/RxMap.ts`（惰性 meta 随葬落地后）—— 首跑 83.87%（156/24/6），**真实检出盲区 2 例已补杀**：五个惰性 meta 的缓存门 `if (this._x) return this._x` 变异恒 miss（每读新建派生结构：订阅翻倍 + destroyResources 只销毁最后一个，先建的全部泄漏——幂等性此前从未被断言）、keys×delete 的 SameValueZero 子句删除（NaN key 在场时 delete 其他 key 误删 keys 列表里的 NaN——weirdNum × RxMap 格子只测过 delete(NaN) 自身，没测过"NaN 在场 × delete 其他"）；均经变异自证（施加 mutant → killer 变红 → 还原复绿），补杀入 round7 资产。no-cov 6 = keys patch 的两个 `assert(false, 'unreachable')` 协议守卫（方法 18 Symbol 隔离后构造性不可达，既有接受类）。`src/RxList.ts`（reorder 载荷深拷 + pairsOwned + meta 随葬落地后）—— **80.07%**（1655 killed / 48 timeout / 389 survived / 35 no-cov，与 round5 后基线 80.05% 持平）：新代码域幸存逐一分类均落入接受项——`pairsOwned` 门变异恒 false = 内部调用方多付一次深拷（快慢路径等价，类 a）、空 order 单例分支删除 = 空数组新分配（观察等价，类 a）、`new Array(n)` 容量提示（digestReplay 先例，类 a）、destroyResources 的行 frame 销毁循环清空 = 被 destroyChildren 构造性遮蔽（rowComputed 同时是 child，类 c 防御分支;僵尸不可达行为已另行钉扎入 round7 资产）。无 (e) 类立案项。
  - 2026-07 round8 发版前复审其余三个改动模块(round7 先例)。`src/computed.ts`(copyTriggerInfoPayload 形状表 + async warn 去重落地后)—— **78.69%**(481 killed / 10 timeout / 125 survived / 8 no-cov,总 624;对比 round7 后基线 77.32%,+1.37pt)。新代码域幸存分类:形状表的 `'clear'` 子句删除 = **真实检出盲区已补杀**(clear 的 entries 在库内无 patch 消费者,一层拷贝只在"先注册 handler 清空 entry 对 × 后注册 handler"双观察者形态可观察——RxMap clear/replace 跨观察者独立性 killer 入 round8 资产,变异自证);`copyNestedArray` 恒数组守卫、reorder 的 argv/reorderInfo 存在性守卫 = 协议内恒真防御(类 c);console 文本(类 d)。`src/RxSet.ts`(forEach track 序修复后)—— **90.56%**(259/0/27/0),与 round7 基线完全持平,track 前移行的变异全部被 R8-6 killer 杀死,幸存与既有接受域相同。`src/notify.ts`(digest 循环诊断落地后)—— **73.75%**(307 killed / 2 timeout / 98 survived / 12 no-cov;对比方法 19 后基线 72.61%,+1.14pt)。新代码域幸存分类:阈值 `Math.max → min` = **真实检出盲区已补杀**(min(初始×16, 100k) 会对 16 步以上的合法级联误报——"小初始队列 + 200 步收敛级联(pending-list 模式)零误报" killer 入 round8 资产,变异自证);`×16 → ÷16` = 诊断时点变异(仅 >6250 初始 effect 的巨型 batch 提前到绝对下限 100k 触发,100k 下限仍挡住一切小 batch 误报,类 a 接受);警告文本(类 d)。
  - 2026-07 round8(方法 24)改动后重审 `src/RxList.ts`(filter 布尔化、swap 重叠 assert、公开 reorder dev 校验、indexBy/toMap 插入侧 assert、行级重算 cleanup)—— **80.46%**(1718 killed / 49 timeout / 394 survived / 35 no-coverage,总 mutant 2196;对比 round7 后基线 80.07%/1655/48/389/35:killed +63)。新代码域幸存逐一分类:公开 reorder 校验的边界子句变异(`>= 0` 删除、`<` 变 `<=`,froms/tos 双侧共 6 个)与 swap 重叠断言的算术变异(`newStart - start` 变 `+`)= **真实检出盲区,已补杀**——单纯 toThrow() 杀不死单子句变异(违规漏到"position sets must match"兄弟断言仍然抛错),killer 改为**消息精确断言**(每用例只违反一个子句)+ 非零起点重叠形态;3 例变异自证(施加 mutant → killer 变红 → 还原复绿)。filter 的 `if (!initialBuildDone) → if(false)` = 等价路径(初建期 pending 恒 null,else 分支同为 push,类 a 接受)。其余幸存与 round7 基线同域(reorder 体的幽灵 SET info 等类 c/d 既有接受项),无新立案。
  - **2026-07 round4 补法轮：幸存分类增加第 (e) 类「套件无法构造触发态」**，与 (a)-(d) 的本质区别是它**不是接受项**：mutant 所在行有覆盖、变异后行为确实不同，但可观察差异只在某个套件从未构造过的状态下出现（R4-1 的 hasPendingStructuralInfos 守卫——变异后仅"读 index 的行 × reorder"或"batch 内先写行依赖再结构操作"可区分，而套件从不构造这两个状态，mutant 在 80% 分数下幸存）。处置 = 补**状态构造**（生成器维度/专项 killer），不是补断言、更不是归入等价接受。判别方法：幸存 mutant 逐个问"什么状态能让这个变异可观察？套件构造过这个状态吗？"——答案是"没构造过"即 (e) 类。本轮补杀：守卫 load-bearing 场景（batch 内先写行依赖再 splice）的三例 killer 进 `mutationKillersRxList.spec.ts`（含变异自证：把守卫条件动态翻转为恒 true——强制直写分支——确认 killer 失败后还原），行形态两列进 broadOperatorsFuzz 常驻。
